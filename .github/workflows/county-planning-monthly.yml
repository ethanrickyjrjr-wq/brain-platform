name: SWFL county planning monthly (Tier 1 cold storage)

on:
  schedule:
    # 14:00 UTC = 10 AM ET on the 3rd of each month.
    - cron: "0 14 3 * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run — scrape and build NDJSON but skip Storage + inventory writes"
        required: false
        default: "false"

permissions:
  contents: read

jobs:
  ingest:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install ingest dependencies
        run: pip install -r ingest/requirements.txt

      - name: Run county_planning_swfl pipeline
        env:
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}
          SPIDER_API_KEY: ${{ secrets.SPIDER_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          DESTINATION__POSTGRES__CREDENTIALS: ${{ secrets.DESTINATION__POSTGRES__CREDENTIALS }}
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python -m ingest.pipelines.county_planning_swfl.pipeline --dry-run
          else
            python -m ingest.pipelines.county_planning_swfl.pipeline
          fi
