# Build Queue — the one human input

> **This is the only hand-maintained status file.** /ops reads it to (1) order the
> REDs ("next up" = top-to-bottom here) and (2) flag YELLOWs (currently building).
> Everything else on /ops is derived from real signals.
>
> Format: priority = line order. `- [x]` done · `- [~]` building now · `- [ ]` up next.
> Edit this file on GitHub; /ops picks it up within 5 minutes.
>
> **⚠ RECONCILIATION (2026-06-21, audit-verified).** All FINAL BOSS Pieces 1–5 (Workspace
> Shell, Project-Aware AI, Signal Layer A+B, Editing/Refresh/Trash, Funnel Arrival) are
> **committed and live on `origin/main` as of `4f42df31`** — `git log @{u}..HEAD` is empty,
> so **nothing is held**. (The prior note here blamed a non-existent "2,278-file commit
> `e9ad3fd`"; that commit is a 3-file hooks change — ignore it.) The Piece 1–5 lines below are
> now flipped to `[x]` with the stale push-prose removed. **Umbrella:** every other
> "PUSH HELD / awaiting push / local-ahead / NOT pushed" marker remaining on this queue is
> likewise stale for the same reason (HEAD == `origin/main`); the genuinely-open work is always
> a PROD deploy/migration gate, tracked as a `*_verify` check in the `checks` ledger — never a push.

- [x] **Email/Social AI lake wiring** (`docs/superpowers/specs/2026-07-01-email-social-lake-wiring-design.md`, plan `docs/superpowers/plans/2026-07-01-email-social-lake-wiring.md`) — corrects the earlier read-only audit's Gap 1 (killed the live per-request RentCast/SteadyAPI calls in `loadListingContext`; RentCast was already dead, no key configured) and Gap 4 (strict tool use was already shipped same-day, only the new field was open). ✅ **BUILT + verified offline (this session):** `data_lake.listing_transitions_recent_zip_stats` view (30d/90d GROUPING SETS, `seed=false` excludes the SteadyAPI cutover baseline — confirmed live via lake MCP the baseline is correctly excluded, zero non-zero-activity rows today since the daily cron has only run once) · `lib/listings/select.ts` `loadListingContext` now reads `data_lake.listing_state` instead of live vendor APIs (zero live vendor calls in the Email/Social Lab request path); `lib/listings/rentcast.ts`'s dead `fetchSaleListings` deleted, `Listing`/`normalizeListing` kept for test fixtures; `lib/listings/steadyapi.ts` untouched (still live for `lib/assistant/comp-helper.ts`) · `loadLifecycleDigest` (`lib/email/market-context.ts`) adaptive 30d/90d window, folded into `fetchLakeParts` · `schedule_suggestion` optional field on `AUTHOR_TOOL`/`AuthorDocSchema`, passed through `authorDoc()`'s payload (UI prefill into `ScheduleSendModal` flagged as a fast-follow, not in this build). Gates: `bunx next build` ✓, full `bun test` **4344/0**, eslint clean on touched files. ⬜ **Live-verify (operator-run):** real listing photo with zero live vendor calls, lifecycle digest line once the cron accumulates real day-over-day data, `scheduleSuggestion` in the author response — check `email_social_lake_wiring_live_verify`.
- [~] **Listing-lake sold capture (Phase-2 Part A — SteadyAPI sole-spine)** (`docs/superpowers/plans/2026-06-30-steadyapi-sole-spine/phase-2-sold-lake-and-comps.md`, spec `docs/superpowers/specs/2026-07-01-steadyapi-sold-capture-design.md`) — organic sold-price capture: when a tracked listing leaves the sweep, one budget-sampled `/property-tax-history` probe resolves it to `sold` (real price + close date) or `withdrawn`. `current_status` is authoritative so pending/ambiguous/API-gap stays `holding` (moat-safe — never fabricates a withdrawal); a holding re-check re-probes aged holdings (age off frozen `last_seen`, NOT `days_in_state`) so pending-then-closed sales accrue. ✅ **BUILT + verified offline (this session):** migration applied to prod (+`sold_price`/`sold_date` on `listing_transitions`, +`sold_check_at` on `listing_state`); `extract_api.classify_off_market`/`fetch_sold_event` + `transitions.plan_off_market_checks`/`apply_off_market_resolutions` + pipeline off-market hook (LIVE api only; dry-run/seed = 0 calls; ~500/mo budget, `list_price desc` skew logged) + `distill.stamp_sold_checked` (targeted UPDATE preserves `last_seen`). Full listing_lifecycle suite **93/0**; load + stamp + upsert round-tripped on live-DB sentinels. ⬜ **Live-verify (operator-run, paid-call gated):** confirm the sold `event_name`/`current_status` enum verbatim + one real sold capture ≤ cap — check `steadyapi_sold_capture_live_verify`. Part B (comp helper) = separate track.
- [x] **Rentals brain (Phase 4 — SteadyAPI sole-spine)** (`docs/superpowers/plans/2026-06-30-steadyapi-sole-spine/phase-4-rentals-brain.md`, spec `docs/superpowers/specs/2026-07-01-active-rentals-swfl-design.md`) — active rental listing inventory from SteadyAPI `/rentals-search`, weekly county-form sweep (Lee + Collier; the plan's proposed per-Lee-city fallback was dropped after a live probe showed the county-form paginates cleanly and covers more than the city list). Renamed from the plan's proposed `rentals-swfl` (collides with the LIVE Zillow-ZORI brain of the same id) to **`active-rentals-swfl`**, mirroring the existing `active-listings-swfl`/`housing-swfl` inventory-vs-index split. ✅ **BUILT + verified offline (this session):** `ingest/pipelines/rentals/` (12 pytest green) + migration `docs/sql/20260701_rentals_swfl_table.sql` (`rental_listings_swfl` + `rental_listing_stats` aggregate view — count + price MIN/MAX only, no invented median) + `active-rentals-swfl` pack/source/test (4 green) + BRAIN_GEO/catalog/vocab/cadence/GHA-wrapper wiring. `bunx next build` ✓; refinery pack suite **423/423**; vocab-coverage clean. ✅ **Live-verify closed (2026-07-02):** migration applied, `ingest-rentals.yml` dispatched `dry_run=false` (run 28559510045), 7,185 real rows landed (Lee 4,018 + Collier 3,167) — `active_rentals_swfl_live_verify` closed. Cron graduated to the weekly `pipelines:` schedule same day. ✅ **Wired into `master.input_brains[]`** (plain `input`, non-critical, deterministic neutral/magnitude-0 reporter — same pattern as `investor-zip-swfl`/`active-listings-swfl`); rebuilt `master` v90, vocab-coverage + catalog test clean.
- [~] **One Assistant — unify the two AI engines** (`docs/superpowers/specs/2026-06-21-one-assistant-unification-RECONCILED-SCOPE.md`) — collapse the two engines behind one button into ONE: `lib/assistant/engine.ts` → `POST /api/assistant`, honest contexts **PROJECT AI / OUTSIDE AI** (+ public funnel). ✅ **Phase 0–2 on `main` (pushed):** harvested surface-contract + resolver + `surfaceNote` + CI guard (from `49e64e3`); the engine + `/api/assistant`; `/api/converse` + `/api/welcome/chat` are **fully deleted** (logic moved into `lib/assistant/{report,conversation}-path.ts`). **No-404 (#11):** resolver degrades every miss (invalid ZIP / unknown kind / missing brain) to the region-wide master read and NEVER throws — converse 404 test inverted (#10), new `report-grounding.test.ts`. Gates: `bun test` **3520/0**, eslint clean, `next build` ✓; legacy route suites pass UNCHANGED. ✅ **as-of MM/DD/YYYY date fix DONE** (`asOfFromToken` live; `freshness_token` internal-only; rule 5 mirrored in `rules-of-engagement.mts`; CI wall `display-leak.test.mts` blocks any re-leak of the raw `SWFL-…-YYYYMMDD` token) — `04e6c4a7` (conversation path #9) + `8bd1874b` (report path). ⬜ **Next:** client flip (dock/panel → `/api/assistant` via one `use-assistant`); charts-in-panel + highlighter-to-root; web-fallback rung (#12). ⬜ **Prod verify after deploy:** check `one_assistant_unify_live_verify`.
- [x] **Project Marketing Hub v2** (`docs/superpowers/plans/2026-06-24-marketing-materials-hub-v2/` ← spec `docs/superpowers/specs/2026-06-24-marketing-materials-hub-v2-design.md`) — template-first materials library: block-canvas emails saved as `deliverables` rows (version-collapse via existing `splitDeliverableVersions`), steerable "what's this for?" intent line + `SEED_DOCS` template rail, list-default library, Update-Data fork (scope `{kind,value}` + `applied===true`), photos bridge (promote filed photos to public `email-media` via native cross-bucket copy on storage-js 2.106.1). 9 tasks (Opus: 1/2/3/5/9 · Sonnet: 4/6/7/8), sequential per the README (dependency chain + shared files — NOT a fan-out). ✅ **Task 1 DONE (`da0daa2d`):** migration live (`deliverables.doc JSONB` + `data_as_of TIMESTAMPTZ` + public `email-media` bucket), `TemplateId`/`buildRenderModel` case, `DeliverableRow` type, `page.tsx` SELECT + `.map()`. Post-Task-1 audit (same session) patched two gaps: block-canvas redirect guard in `app/p/[id]/page.tsx` (was a 500, now redirects to email-lab) + `"block-canvas": "Email"` in `template-labels.ts`. ✅ **Task 4 DONE (`30afa82b`):** `POST /api/projects/[id]/ai-material { intent }` → `201 { id, template }` — keyword heuristic (`pick-seed.ts`, 5 TDD cases) maps free-text to a seed; AI fills via `/api/email-lab/ai`; `applied===true` gate; never a dead end. `crypto.randomUUID()`, service-role write after cookie-client ownership check. ✅ **Task 2 DONE (`42e83239`):** `POST /api/projects/[id]/materials` (create from seed) + `PATCH /api/projects/[id]/materials/[did]` (in-place save, no new version); service-role write after cookie-client ownership check; 23 route tests. ✅ **Task 3 DONE (`fab3745f`):** `POST /api/projects/[id]/materials/[did]/refresh` — Update-Data fork: explicit cross-user **403 ownership guard** (`deliverables` SELECT is public `USING (true)`, so RLS won't scope the read — mirrors `app/api/deliverables/[id]/refresh/route.ts:40`), report templates delegate to `/api/deliverables/[id]/refresh`, block-canvas calls `/api/email-lab/ai` with `scope:{kind,value}` + `applied===true` gate + `EmailDocSchema` re-validate → service-role insert with `supersedes_id`; `mode?:string` label added to the AI route body. ✅ **Task 6 DONE (`6b4afd28`):** `lib/deliverable/material-status.ts` pure `getMaterialStatus` + `getFormatBadge`, 43 tests. ✅ **Task 5 DONE (`15cb830b`):** email-lab save/load. ✅ **Tasks 7+8 DONE (`19ddd2e7`):** photos bridge + hub UI (IntentLine/TemplateRail/MaterialRow/MaterialsHub). ✅ **Task 9 DONE:** `<MaterialsHub>` wired into `ProjectWorkspace` (intent→`/ai-material`, ↻→`/materials/[did]/refresh`, `materials`=heads-with-versions), `DeliverableLanes`→schedules-only ("Scheduled sends"), filed-data board collapsed via new `ui_state.materials_filed_collapsed`; orphaned modal/edit/refresh/revoke handlers removed. Post-build line-by-line audit of Task 8 fixed 2 real bugs: `deriveTitle` now field-precedence (was returning the header brand tagline for EVERY material — seeds are header-first) + row keyboard-activation scoped to `currentTarget` (Enter on inner buttons was navigating). Build ✓, `bun test` 3717/0. Chain COMPLETE: `1✅→(2✅,3✅,6✅)→4✅→5✅→7✅→8✅→9✅`. ⬜ **Manual prod-verify after deploy:** acceptance walk (intent builds a material, seed opens lab, Save creates a row, ↻ forks "Updated N×", >30d amber, filed collapse persists).
- [x] **FINAL BOSS Piece 1 — Workspace Shell (foundation slices)** (`docs/_archive/superseded/final-boss/01-piece-1-workspace-shell.md`) — **scope: Piece 1 ONLY** (operator-confirmed). ✅ **DONE this session (no-UI/foundation):** G2 branding-follows-all-paths (`lib/project/apply-brand.ts` → POST + import + claim) · G4 email+scope through the build path (`lib/deliverable/parse-scope.ts` → web `/api/projects/[id]/build` + MCP `swfl_project_build`; `TEMPLATE_ENUM` now includes `"email"`) · §H `projects.ui_state jsonb` migration **applied to prod** + PATCH branch · §C pure card seams `summarizeItem` (11✓) + `groupItemsByKind` (4✓) + parse-scope (6✓) + apply-brand (3✓). G3 was a **phantom** — `deliverables.scope_*` confirmed live (assembleDeliverable always wrote them). Foundation pushed (`56f3b22`). ✅ **§A SHELL DECOMPOSITION DONE (on `main`):** 743-line `ProjectDetail.tsx` DELETED → `ProjectWorkspace` orchestrator + `workspace/{ItemDetail,ItemCard,ItemsBoard,ProjectTitle,BrandingBlock,ConnectMcpBlock,BuildActions,DeliverableLanes,types}` + grouped cards (`groupItemsByKind`/`summarizeItem`, click-to-expand, within-kind reorder via tested `lib/project/reorder.ts`) + persistent `app/project/layout.tsx` + `ProjectsRail` (master-detail spine, no AI remount, no `key={pathname}`) + slimmed list page. lib/project **32/0**, real-tsc 0, eslint + `next build` green; behavior-preserving. ✅ **§D/§E/§F/§G/§I DONE (on `main`):** §D `components/ui/Modal.tsx` + Built-lane mini-render thumbnails (`DeliverableThumbnail`) → open-big iframe modal (`DeliverableModal`) + schedule-driven Emailing lane (`page.tsx` loads `email_schedules`); pill suppressed on `/p/*` so the modal stays clean · §E `ConnectMcpBlock` 3 modes (open/dismissed-≥2×/connected) via `ui_state.mcp_dismissed_count` + disconnect-confirm; branding collapses on success + for color-key brands · §F `ProjectSearch` pinned in the layout bottom bar (BRAIN_CATALOG + saved_charts), "Add" feeds the workspace via an in-session event (sole-writer, no dual-PATCH data loss) · §G `deriveProjectName` (grounded ZIP→place crosswalk → "Fort Myers Beach 33931") wired into import + claim · §I `?seed=` → one-click seed banner (no LLM auto-fire — P2). Added `key={project.id}` so project-switch remounts the pane (App Router param-nav doesn't remount by default). Adversarial review (15 agents): 11 findings all fixed. Gates: real-tsc 0, eslint clean, `next build` ✓, full `bun test` **2754/0**. ⬜ **Prod verify after deploy:** check `piece1_workspace_shell_verify`.
- [x] **FINAL BOSS Piece 2 — Project-Aware AI** (`docs/_archive/superseded/final-boss/02-piece-2-project-aware-ai.md`, spec `docs/superpowers/specs/2026-06-17-piece2-project-aware-ai-design.md`) — **scope: Piece 2** (operator: "Start Project 02; build correctly, improve where you can"). 7 commits on `main` (pushed). ✅ **DONE (deterministic core, J2 live):** seam-verified P1 first (the plan's bus/PillPage deps were UNbuilt — built from scratch) · context bus (`ai-context-store` + `useAiContext` `useSyncExternalStore` + `ProjectAiContextBridge`) · `inferScopeFromItems` (one scope root) · project `PillPage` kind · `digest.ts` (pure) · `cross-project-index` (reuse/gap) · `prompt-engine` (3+1) · **J2 live** (pill shows project prompts, survives switches) · **§D** project-grounded answers (`describePage` names the project; route UNCHANGED/anonymous) · **§E** assemble-on-command (`planAssembly` + `/api/projects/assemble`, mirrors import). Adversarial self-review (36 agents, 21 confirmed) → fixed incl. 1 MAJOR (rename-staleness in `computeRev`). real-tsc 0, eslint clean (touched files), full `bun test` **2841/0**. ⬜ **DEFERRED:** §8 `ui_state` write-back (dismiss UI) · §9 gated LLM accelerators (paved; deterministic ON) · **G1** authed action surface for J4 (operator decision — rec: option-b-as-wiring). ⬜ **Prod verify after deploy:** open `piece2_*_live_verify`.
- [x] **FINAL BOSS Piece 3 — Signal Layer (Track A + B)** (`docs/_archive/superseded/final-boss/03-piece-3-signal-layer.md` — doc IS the spec) — **one piece / two PRs** (operator-confirmed 2026-06-17). ✅ **Track A BUILT + audited (on `main`):** migration `docs/sql/20260617_project_feed.sql` **applied to prod** (RLS owner-all proven live: owner sees row, non-owner 0) · `lib/project/feed.ts` (`writeFeed`/`readProjectFeed`/`markFeedSeen` + pure `feedRowMatches`; never-throws; `dedup_key` UNIQUE) · `lib/project/project-scope.ts` (`projectScopeSet`+`zipsForPlace`) · `identityKeyForItem` reused (already P2's cross-project overlap key) · `outside-action` emit at claim+import **birth** · 3 additive P2 wires (`digest.feedSignals`+`computeRev` basis · ONE capped prompt between freshData/staleMetric · `markFeedSeen`/PATCH `seen_feed_ids` seam). Adversarial audit (5 agents, most suspicions refuted) → **fixed**: 1 pre-existing P2 build-break (`corridor-display.mts` leaked `node:fs` into the `/project` client bundle, broke `next build` — proven NOT Track A), 1 MAJOR (`dedup_key` namespaced by `project_id` — import-retry signal-loss), 2 minor (`.or()` reserved-char quoting; `feedRowMatches` made load-bearing). Gates: tsc 0, eslint clean, full `bun test` **2899/0**, `next build` ✓. ✅ **Track B BUILT + audited (on `main`):** the `data-change` cron — pure core `lib/project/change-detection.ts` (`ZIP_SIGNAL_BRAINS`=`housing-swfl.median_sale_price`+`rentals-swfl.rent_index_latest`, `decideDataChange`, `buildScopeUserMap`, `indexPriorSnapshots`, `dataChangeDedupKey`) + tests · runner `scripts/project-feed/change-detection.mts` (service-role adapter, `--dry-run`=read-only probe, exit codes) · GHA `project-feed-change-detection-daily.yml` (08:30 UTC, dry_run) · `cadence_registry.yaml` documented probe-EXCLUDED (event-driven write-back; zero rows on quiet days would false-alarm the daily probe) · additive `read_at?` on `FeedRowInput`. Material gate = `reconcileMetric(current cell, prior snapshot)`; the feed row IS the snapshot (no last-seen store). Adversarial audit (2 agents) → fixed **CRITICAL** (`home-values-swfl`/`investor-zip-swfl` absent from `BRAIN_CATALOG` + no stamped `expires` → reconcile `not_found` → silent dead signal; repointed to catalog-resolvable `housing-swfl.median_sale_price`, 125 ZIP rows, + a runner silent-death tripwire), **MAJOR** (reconcile delta sign inverted on a rise → added correctly-signed `change_pct`; title stays the reconcile reason), **minor** (08:00→08:30 slot + comment). `dedup_key` now `datachange:<user>:<zip>:<brain>:<token>` (corrects the spec's user-less key so the per-user fan-out survives the global UNIQUE index). Gates: tsc 0, eslint clean, full `bun test` **2915/0**, `next build` ✓. ⬜ **PROBE FIRST: operator runs** `bun scripts/project-feed/_probe.mts` (untracked) or `--dry-run` before the first real run. ⬜ **DEFERRED:** engagement/external-event/platform-feature · dismiss BUTTON (check `piece3_dismiss_ui`). ⬜ **Prod verify after deploy:** checks `piece3_track_a_verify` + `piece3_track_b_verify`.
- [x] **FINAL BOSS Piece 4 — Editing + Live Refresh + Trash** (`docs/_archive/superseded/final-boss/04-piece-4-editing-refresh-trash.md` — RESOLVED PLAN IS the spec; operator: "build it, don't re-brainstorm") — **on `main`**. ✅ **BUILT:** migration `docs/sql/20260617_deliverables_soft_delete.sql` **applied to prod + verified** (`deleted_at`+`supersedes_id`+partial idx; runner `scripts/apply_deliverables_soft_delete.py`) · routes `app/api/deliverables/[id]/{trash,refresh,edit}` (mirror revoke owner-gate; ALL FREE — send is the only paywall) · `assembleDeliverable` additive `supersedesId` · refresh = same items/today's data (project∩snapshot, snapshot fallback; `lib/deliverable/{refresh-items,resolve-refresh-items}.ts`) · frozen-link integrity (content/refresh FORK + supersedes_id, cosmetic in place; `/p/[id]` page+print 404 trashed) · retention sweep `scripts/deliverables/retention-sweep.mts` + GHA `deliverables-retention-sweep-daily.yml` (09:15 UTC, --dry-run probe) + `cadence_registry.yaml` probe-EXCLUDED write-back · UI: `DeliverableModal` Refresh/Edit/Delete + `DeliverableEditPanel` + Built-lane "Recently deleted"/Restore + per-card versions disclosure + `splitDeliverableVersions`. Adversarial review (Workflow, 24 agents, 6 dims → refute): **12 confirmed → 10 fixed** (email-boundary frozen-link break, frame re-bind fidelity, modal-flash, double-submit, generateMetadata leak, version diamond, unbounded query→selectAllPaged); **2 NOT locked per operator** ("client supplied data can be used by a client — we are not the police") — edit accepts the client's own items; kept only the cross-USER file boundary. Gates: real tsc 0, eslint clean, `next build` ✓, full `bun test` **2978/0**. ⬜ **Prod verify after deploy:** check `piece4_edit_refresh_trash_verify`.
- [x] **FINAL BOSS Piece 5 — Funnel Arrival → Takeover** (`docs/_archive/superseded/final-boss/05-HANDOFF.md` + `05-funnel-arrival-and-takeover.md`) — enroll → branded `/welcome` arrival → claim-token carries brand+seed → ZIP-seeded project. SHIPPED + on `main` (`ed6199bd`/`3863c143`/`58729d4c`/`1f633cd1`): `lib/claim/claim-store.ts`, `lib/prospects/build-arrival-url.ts`, `app/api/prospect/open-project/route.ts`, `scripts/email/enroll-prospect.mts`. Takeover arc + Phase-D live send DEFERRED. ⬜ **Prod verify:** check `funnel_arrival_claim_seed_verify` (due 2026-06-22).
- [~] **Operator Bulk Cold-Outreach DRIP engine** (`_AUDIT_AND_ROADMAP/HANDOFF-outreach-engine.md`) — recurring branded drip: per-recipient ONE chart + brief explainer in the RECIPIENT's scraped brand, "create your own" click-back that auto-populates their colors; click → stop the drip. Operator design locked: CLI-first, ZIP report now (report-type pluggable later), no map, crawl4ai design-sourcing in parallel. ✅ **Increment 1 DONE + pushed (pure core + DRY-RUN CLI, RULE-1-safe):** `lib/email/outreach/{targets,drip-email,campaign}.ts` (+ bun:tests 16/0) reuse the built-but-unwired `renderChart` + `brandThemeToTokens` + `buildArrivalUrl`; new `templates/html/email/email-outreach.html` shell + registry `outreach` slug; `scripts/email/outreach-campaign.mts` reads a CSV, scrapes brand (confidence<0.5 → SWFL house brand, never guessed colors), assembles per-ZIP content via `assembleActivationReport` (6-county MOAT gate), writes a timestamped run-report + previews under `outreach-runs/` (gitignored), live send GATED. Smoke: 33931→READY, 90210→OUT_OF_SCOPE. ⬜ **Increment 2 (live, RULE-1 diff-review):** per-recipient unsubscribe identity + click→suppress (Resend webhook) + verified CAN-SPAM sender + secrets → `resend.batch.send`; drip enrollment table + cadence (generalize `lib/email/activation` + `scheduler`). ⬜ **v2:** trend sparkline off `lib/charts` series; crawl4ai design recreate (env-blocked here — ready-to-run plan in the design agent's output).
- [~] **Social Campaign Builder** (`SOCIAL BUILD/` + spec `docs/superpowers/specs/2026-06-20-social-auto-posting-design.md`) — **OUR SIDE 01–06 COMPLETE + PUSHED to origin/main.** Two go-live fixes queued for handoff in `SOCIAL BUILD/07-go-live-prepost-fixes.md` (checks `social_media_storage_upload` = wire rendered-PNG→public `social-media` bucket; `social_x_media_v2_scope_verify` = land the concurrent X-v2 fix `7b0f588e` + `media.write` scope in U1) — done = fix → DRY-run/verify. Stage 3 pushed selective-lineage (`57a2f214`+`6f8d8f97`+`7ff205fe`+handoff) excluding 5 concurrent commits on shared main (incl. `cb5e4acc` cross-project-AI held for diff-review). Stage 2 pushed (`577fecc6..f7f1d541`). Stage 3: `04` cron worker + `social-scheduler.yml` (`57a2f214`, freshness-gate + `claimSocialOnce` at-most-once + DRY publish-gate + reaper + exit-code contract; cron paused until go-live) ‖ `06` engagement polling = `engagement.ts`+`channels/metrics.ts`+`poll-engagement.mts`+`social-engagement-poll.yml` (`6f8d8f97`, built own per-platform metrics fetchers since 03 had no read path; dedup `(social_post_id,metric)`, empty-tolerant; **Vendor-First: GBP per-post insights API REMOVED→no-op, X clicks non-public, LinkedIn org-only, FB no comment/share post metric**). Integrated gates: real-tsc 0, eslint 0, `bun test lib/social/ scripts/social/ lib/deliverable/` **456/0**. **⚠ Before go-live:** wire rendered-PNG→CDN upload (04 `mediaUrl` null in v1) + re-verify 03 X media path (v1.1 sunset→v2 `/2/media/upload`). Stage 2: `03` adapters (X/Meta/LinkedIn/GBP) + AES-256-GCM token store (`aeec3ac4`, vendor-verified live, new env `SDG_CRYPTO_KEY` + per-platform client secrets — operator-owned) ‖ `05` "social" deliverable template + footprint grain-guard (`2c4319fc`, zero regressions). Mirror of the email/outreach engine for client social auto-posting: clients connect own accounts (OAuth) → store+refresh tokens → cron posts branded graphics on a freshness-gated cadence at zip/place/county grain, "just by asking AI." **Direct APIs, NO paid middleman** (X, Facebook+Instagram, LinkedIn, Google Business Profile); whole pipeline DRY behind `SOCIAL_PUBLISH_ENABLED` → one flip to go live when payments in. ✅ **Stage 1 DONE (cherry-picked onto main `8ef162f4`+`f4a182a5`, HELD for push — new dep + live `/api/social/render` + applied migration → RULE-1 diff-review):** `01` spine = `lib/social/types.ts` + 6 pure DI cores + `idempotency.ts` + `scripts/social.mjs` go-live switch; migration `docs/sql/20260620_social_schema.sql` **applied to prod + verified** (5 tables + metrics view + `claim_due_social_schedules` RPC + RLS + unique idx). `02` rasterizer = `lib/social/render-social-image.ts` + `app/api/social/render/[format]/route.ts` via **`@resvg/resvg-js@2.6.2`** (whole-card SVG→PNG Buffer; `next/og`/Satori rejected for the cron's direct-Buffer path), branded + burned-in watermark + no-fabrication tripwire. Gates: real-tsc 0, `bun test lib/social/` **79/0**, `next build` ✓, lockfile frozen-safe. ✅ **Stage 2 PUSHED** (`03` adapters+token-refresh ‖ `05` social template). ✅ **Stage 3 PUSHED** (`04` cron worker ‖ `06` engagement; 2 go-live fixes in `07`). USER SIDE = `SOCIAL BUILD/USER-SIDE-HANDOFF.md` (hand to another Claude to plan). Research: `ingest/pipelines/social_best_practices/crawl_social_practices.py` (run where web is open). ⬜ **App-review track (X / Meta ×2 / LinkedIn / GBP) starts day 1 — the long pole** (weeks each; operator-owned).
- [~] **Rich `/r/zip-report`** (operator: "one page on a zip won't work — charts, graphs, commentary, 4 sections, map"). ✅ metro home-value trend chart (`MetroAreaChart`/`zhvi_pivoted`, guarded loader) in the SWFL section; 4 grain sections intact. ✅ **Quick data summary now LIVE** — `data_lake.census_acs_zcta` (100 ZCTAs, ACS 5-year) → `lib/zip-summary/load.ts` renders cited demographic figures (pop/income/age/owner-occ/poverty/employment/HH/mobility; source data.census.gov, as-of 12/31/2022). Pipeline `ingest/pipelines/census_acs/` + GHA `census-acs-annual.yml` + cadence entry. Prod-verify: check `zip_quick_summary_live_verify`. ⬜ **NEXT — crawl4ai current-data pass:** add cited *current* figures NOT already in the lake (housing/rents already covered) — candidate per-ZIP new-construction permit counts (confirm not already in permits-swfl), active inventory, school ratings; brainstorm + design doc first. ⬜ **MAP — LEFT OPEN (operator: "still want a map, we will add"):** ZIP choropleth pulled from the client-facing report because the served contractor `/maps/lee-collier.svg` welds Fort Myers Beach (33931) to the mainland. `lib/report/zip-choropleth-data.ts` (+test) + `ZipChoropleth` kept ready — re-add the County-section block ONLY after the corrected contractor SVG lands (re-run `scripts/clean-contractor-map.mjs`) and 33931 is verified as a real island. ⬜ more per-section charts (rents/tier) · per-recipient brand on the page · richer commentary.
- [x] **realtor.com market-heat-swfl brain (ZIP-grain inventory/DOM/pending)** — BUILT 2026-06-25 (Tier-1 parquet sibling of seller-stress; vote = sign+magnitude off `_yy`, Hotness relative-only, falsifier off History, list-side only; gates: pack 25/25, py 8/8, vocab `--all` + catalog 4/4 + refinery render + `bunx next build` ✓; spec `docs/superpowers/specs/2026-06-25-market-heat-swfl-design.md`). ⬜ First ingest run + prod-verify after deploy. Source scan done + verified live 2026-06-25 (memory `reference_realtor-data-library` + `reference_real-estate-data-source-scan`). Free public-S3 CSVs (`econdata.s3-us-west-2.amazonaws.com/Reports/Core/RDC_Inventory_Core_Metrics_Zip.csv` + `…/Hotness/…_Zip_History.csv`), ZIP/county grain, attribution-only license, monthly cadence. Verified: pulled real 33901/33904/34102 rows. Columns: median_listing_price, active_listing_count, median_days_on_market, new_listing_count, price_reduced_count/share, pending_ratio, $/sqft, +M/M +Y/Y + Hotness score/rank. **Shape = Operation Dumbo Drop:** SWFL-ZIP-filtered pull (`fixtures/swfl-zip-county.json`) → DuckDB → Tier-2 table (REPLACE not append — they restate history monthly) → empty-tolerant consuming brain in same PR (brain-first ingest gate). GAP: list-side only, NO closed/sold prices (that's ATTOM lane). Directly feeds the open "crawl4ai current-data pass" on the Rich /r/zip-report item below. ⬜ **Brainstorm + design doc before building** (RULE 3.5). Rejected alts: Repliers (MLS multi-tenant ban), Bridge-pub (ZG-gated).
- [x] Output format sweep A3–A6 (`docs/superpowers/plans/2026-06-15-root-R4R5-visuals-discoverability.md`): A3 BRAIN_GEO grain-order fix (storm/hurricane region-first so SWFL rollup isn't labeled "Lee county-wide"); A4 shared `lib/format-metric.ts` + `harvestMetricItems` now emits `$400,000` not `400000`; A5 cre-swfl citation truncated to 3+N-more (no per-corridor URLs) + labor-demand URL removed from prose; A6 `asOfFromToken` → MM/DD/YYYY. 2563/0 tests.
- [x] Daily Freshness System — Wave 1 (brain-first atomic PR, `docs/superpowers/plans/2026-06-15-daily-freshness-system/`). `data_lake.daily_truth` migration + `ingest/pipelines/live_search/` cascade engine (Gemini→Firecrawl real legs; Spider/Claude honest-None stubs) + pipeline + `cadence_registry.yaml` `live_search_config:` entries (W1–W3); `freshness-pulse` Tier-1 reporter brain + `daily-truth-source` connector + tests + registration (index/catalog/master `modifier` edge/vocab) + empty-tolerant `brains/freshness-pulse.md` (W4–W6). MOAT 3-gate (value+source_url+!anomaly); no LLM in math path. 15 py + 10 pack + Gate-5 + vocab `--all` green.
- [x] Section 1 — stamp THE-GOAL.md + lean rules-of-engagement block
- [x] Section 2 — /ops live operations ledger (this dashboard)
- [x] Apply predictions/outcomes SQL to live Supabase + verify a prediction row lands
- [x] fl_dor_sales_tax — run schema migration + first backfill, move registry to active
- [x] Section 3 — plan master synthesizer flesh, starting from /ops state
- [x] Fix US-41 / Tamiami Trail corridor naming collision
- [x] safety-swfl — FBI Crime Data Explorer replaces unfit FIBRS (#59); brain LIVE (bullish, -9.7% YoY), `public.fdle_crime_swfl` backfilled 2022–2024, quarterly cron re-enabled, master v71
- [x] Highlighter Phase 1 — in-page ask layer (`docs/superpowers/specs/2026-06-07-highlighter-in-page-ask-chart-design.md`): fact detection + mobile chips, popup (3 states), `/api/converse` (haiku, metered, enforcement OFF), precomputed suggestions, discovery coachmark
- [x] Highlighter Phase 1.5 — /r/ AI answer-quality pass (`docs/superpowers/plans/2026-06-09-highlighter-iteration.md`): header-badge grounding, 3-lane be-Claude/grounded/offer prompt, slug→label CLEAN fix, FOCUS/NATURAL/no-echo/concise voice, type-aware chips (killed "what's driving <date/token>"), chart/section real-text fix, off-screen shield, SWFL-wide platform framing — all `/r/` pages
- [x] Highlighter Phase 2 — real-time follow-up prompts + selection-type awareness (`docs/superpowers/plans/2026-06-09-highlighter-iteration.md`): `⟦FOLLOWUPS⟧` same-call tail split client-side (`splitFollowupTail`), `deriveSelectionType` (section/token/date/place/metric) passed to converse — directive GATED on selection_type (dock spends no tokens); + mobile number tap-targets (`MetricValueCell` string|number, `DataRow` FactChip) + chip analytics (`recordAsk` → `data_requests.selection_type/is_realtime/from_chip`, columns live). Tests 64✓, tsc/eslint clean. Live runtime verify (model emits tail, chips render, mobile tap) pending → check `highlighter_realtime_prompts` stays open.
- [x] §D2 — Universal Location Search endpoints: `GET /api/where?q=` (any input → dossier, plain text or JSON) + `GET /api/z/[zip]` canonical ZIP permalink
- [x] §D1 — MCP `swfl_fetch` zip fan-out: `resolveLocation` + `assembleLocationDossier` for zip/location params (no explicit `report_id`); pinned non-master `report_id` keeps the single-brain `fetchDetailRow` drill (back-compat). Tier-2 default, capped (true-ZIP + 8 headline); `_meta.dossier` = full `LocationDossier` matching `/api/z?format=json`. 5 integration tests green
- [x] §D3 — search box (any input → redirect) + identity header card + grain chips + did-you-mean; generalized `/r/zip-report/[zip]/page.tsx` off `assembleLocationDossier`. New `/r` index + `/r/search` resolver + shared `lib/location-surface.ts` (19 tests). Also fixed `place-resolver.mts` `import.meta.dirname` impurity that threw on `resolveLocation` in the Next/Vercel bundle (was latent on D2's corridor path too — `/api/where?q=North Naples` now 200). §D COMPLETE
- [x] §E — address geocoder: `refinery/lib/geocode.mts` `geocodeAddress(q)` (Mapbox v6 forward primary + Census fallback; URL-restricted token → explicit `Referer` header, works on Vercel; locality fall-through reverse-geocodes `types=postcode`). Wired into `location-resolver.mts` step 6 with `resolveZip().in_scope` (6-county ⊃ METRO_4) scope gate. + `LocationDossier.coverage_caveats` surfaces the Charlotte/Sarasota housing-only asymmetry as a stated boundary (not silent thinning) in `/api/where`+`/api/z` JSON + MCP `_meta.dossier`. 23+52 tests green. LIVE-VERIFIED 2026-06-10: address→33908, Pelican Bay→34108, Charlotte→33950+caveat, Sarasota→34237; `connector_output_live_verify` + `mcp_zip_fanout_live_verify` CLOSED
- [x] Highlighter — session persistence (thread lifted to `HighlighterProvider`, survives close/reopen, shared popup⇄dock) + briefcase capture tray (`localStorage` draft project, `ProjectItem` spine) + "File this answer/figure/report" + cross-cell snapping w/ ambiguous-mix suppression (Projects S1, `…/session-1-highlighter-thread-briefcase__OPUS/`). Per-brain `/r/` tuning tail folds into later sessions.
- [x] Charts Tier A — deterministic at-a-glance chart from key_metrics/detail_tables (`docs/superpowers/specs/2026-06-07-chart-generation-three-tier-design.md`); wire the dead `Dossier.chart` slot + new `/r/` render child
- [x] Charts Tier B + Highlighter "Chart this" — build `buildChartForIntent` (routeChart has NO consumer today) + `HBarChart` responsive fix (shared sub-task)
- [x] `/c/[id]` saved chart → `/project/[id]` (first `auth.uid()` RLS policy) → PDF via `window.print()` — Projects + Briefcase S0–S5 (`docs/superpowers/plans/2026-06-10-projects-briefcase-assembly/`)
- [x] Assembly engine + hosted deliverable — `POST /api/projects/[id]/build` (forced-tool Sonnet, narrative-only) + `/p/[id]` public page (source + as-of under every exhibit) + EXACT-anchor moat lint (regenerate-then-strip, adversarially hardened) + deterministic jargon scrub + restyle-without-re-LLM (S6, `…/session-6-assembly-engine__OPUS/`). 71 deliverable tests green, tsc clean. Shipped to `main` (build route + system prompt). Live 4-template build (<8s) + `deliverable_anchor_lint` close are post-deploy. S7's `aaf7a10` revoke route now has its `/p/[id]` dependency.
- [x] Delivery + revoke — email draft / share-sheet / mailto; `/p/[id]` 410 on revoke; owner kill switch (S7, `0409888`)
- [x] Uploads v1 — `project-uploads` Storage bucket + RLS path-prefix; attach+caption (10 MB/file, 10/project); cross-user read DENIED (S8). Bucket+RLS LIVE in prod; `storage_rls_scope_verify` CLOSED on a live two-account test (8/8: cross-read/sign/write + anon + expiry all DENIED, owner-reads-own positive control). UI shipped to `main`; deploy pending.
- [x] Presentation Deliverable Engine (`docs/superpowers/plans/2026-06-10-presentation-deliverable-engine/`) — Phase 0–6 ALL ✅ deployed (`4be4405`/`f6d9a02`/`69e076b` on main). Phase 6 (brand theming): `lib/deliverable/brand-theme.ts` (`extractBrandTheme`/`toChartTheme`); theme injected at render time in `/p/[id]`; `FrameRenderer` CSS custom props (`--chart-primary`/`--chart-accent`); logo in branding slot; 136 deliverable tests, tsc 0.
- [~] **Briefcase Everywhere — Plan A (Front Door)** (`docs/superpowers/plans/2026-06-15-briefcase-everywhere/A/`) — all 9 tasks built (A-1 `/api/me`+`useSession` · A-2 root `BriefcaseProvider` · A-3 ONE unified pill, FAB+tray retired, dock preserved on `/r/*` · A-4 live `/p/example-*` engine-dogfooded · A-5 state-branching panel · A-6 DRY chat-stream · A-7 anon-visit context prompts · A-8 Open-project link fix · A-8.5 `usage_events.user_id`=`auth.uid`). Suite **2441/0**, lint + `next build` green (routes static). A-1/2/4/6/7/8/8.5 ON ORIGIN (concurrent fast-forward to `fbc6dbb`); A-5 + A-3 local-ahead (push pending operator). ⬜ **PROD HELD (operator gate):** `is_example` migration + seed 4 examples + `check.mjs` ledger (`briefcase_examples_live_verify`, A-8.5 prod-migration check, close `meter_uid_attribution`). A-8.5 `user_id` migration applied. Tier-2 deferred: watermark render, send paywall, pill-chat⇄report-thread `use-converse` bridge.
- [~] **Briefcase Everywhere — Plan B (Carry-Back Bridge)** (`docs/superpowers/plans/2026-06-15-briefcase-everywhere/B/`) — all 7 tasks built (B-1 `claim_tokens` table + atomic server-side `consume_claim_token` RPC + `lib/claim/claim-store.ts` · B-2 keyless `swfl_project_handoff` MCP tool, **`swfl_fetch` UNTOUCHED** · B-3a `/api/claim` consume + cookie-client RLS-bound insert, deterministic token-derived id, race-safe · B-3b `/claim` non-consuming preview + `ClaimOnLogin` auto-claim · B-4 `isMcpConnected` derive helper, NO binding table · B-5 RESPONSE_CONTRACT carry-back nudge). Suite **2467/0**, eslint clean, `next build` green (`/claim` + `/api/claim` present). Migration **APPLIED to prod** (table+RLS+RPC+grants verified; anon/auth DENIED; consume RPC live-validated in a rolled-back txn). **6/6 adversarial security invariants hold.** ⬜ **PUSH HELD (operator gate — MCP surface + live `/api/claim`):** awaiting diff review; committed locally. Live-verify check `carry_back_bridge_live_verify` open (real anon handoff → claim → `/project/{id}` post-deploy). Deviation from B-1's literal SQL: consume is an RPC (server-side `now()`), not a PostgREST `.update()` chain.
- [~] **Briefcase Everywhere — Plan C (Reconciliation Engine)** (`docs/superpowers/plans/2026-06-15-briefcase-everywhere/C/`) — lane-3 verdict engine BUILT C-1..C-6 (TDD, ~95 tests; full suite **2535/0**, tsc/eslint clean, `next build` TS-clean). C-1 `expiresFor`+`freshnessGate` (only reject primitive, fail-closed) + Stage-4 `output.expires` stamp (additive). C-2 pure `reconcileMetric` + 5-status verdict (stale → value WITHHELD; uncataloged → `not_found` not false-stale). C-3 `resolveMetricSlug` + catalog-gap-safe `lookupLakeFact` (per-ZIP reads structured `detail_tables`) + `toAssertion`. C-4 narrative-lint `"ttl"` gate behind `RECONCILE_TTL_GATE_ENABLED` (default OFF, byte-identical via pure `gateNarrative`). C-5 keyless `swfl_reconcile` MCP tool (`swfl_fetch` untouched) + verdict renderer. C-6 end-to-end test + 16-agent adversarial review (11 findings fixed: live MCP slug-leak, build.ts seam/testability, strict-numeric compare, ttl collision+categorical, honest `unknown` grain). `asserted_grain` dead-branch fixed: `4b0e418` wires param through tool inputSchema → LaneTwoAssertion (gate 4 now reachable). Commits `636f7ec`..`4b0e418` — **awaiting push** (operator diff-review: MCP surface + brain-output types + Stage-4). ⬜ **operator-gated:** FULL rebuild (stamp `expires` on every brain) → flip `RECONCILE_TTL_GATE_ENABLED` ON → push; checks `reconciliation_engine_live_verify`/`reconcile_ttl_gate_flip`/`reconcile_live_lane1_rewire`.
- [~] **Briefcase Task 4 — "email" deliverable + PDF doc skin** (`docs/superpowers/specs/2026-06-16-task4-briefcase-email-pdf-design.md`, AS-BUILT) — new `"email"` TemplateId renders via the Task-2 grounded spine (`buildEmailDeliverableModel`→`renderGroundedReport`), NOT `buildRenderModel`: pure/deterministic over the frozen snapshot (ZIP-only via shared `resolveReportZip`; metrics from `items_snapshot`; **reads from narrative prose, not a metric echo**; `freshness_token` lifted from items; `captured_at`=`row.created_at`). `renderSkin` routes `skin:"pdf"`→new `doc-report` slug (`templates/html/email/doc-report.html`, letter `@page`, no CTA, watermark; email skin byte-identical/golden-equivalence green). `/p/[id]` email branch = `<iframe srcDoc>` + Save-as-PDF→deliverable-keyed print route `app/p/[id]/print/route.ts` (auto-print); `GlobalDigestFallback` for non-ZIP/legacy. Atomic type-lift (commit `48ef6eb`): TemplateId + `buildRenderModel` case + `DELIVERABLE_TEMPLATES` + `assembleDeliverable` scope opts/INSERT + `example-email` (housing-swfl/33901, inline upsert). lib/deliverable **198/0**, lib/email **356/0**, tsc/eslint clean. ⬜ **operator-gated:** `docs/sql/20260616_deliverables_scope.sql` (ADD scope_kind/scope_value) applied post-push; remaining for live-verify = Vercel deploy + example-email rebuild + `/p/example-email` paints. Check `briefcase_email_pdf_deliverable` open.
- [~] **Root 1 — unify the AI + project flow (R1 + BTN-1)** (`docs/superpowers/plans/2026-06-15-root-R1-unify-ai-project.md`) — the "50 different sites / prompts produce shit / can't build anything" root. SHIPPED steps 1–7 this session (**bun test 2569/0**, lint + `next build` green, routes static): `/api/welcome/chat` gains `mode:"analyst"|"summarize"` (default `welcome` = public landing UNCHANGED). Analyst no-location → `buildAnalystSystem` grounds on the master read (`fetchBrain("master")`→`buildDossier`→exported `renderBlock`; failsafe to un-grounded analyst premise). Analyst location → `voice:"analyst"` threaded through `buildWelcomeGroundedSystem`/`welcomeGroundedSpeakLine` (file-into-project close, not the email hook). `useChatStream` passes extra body fields (spread-before-`messages`). `BriefcaseChat` runs analyst mode + teal "File this answer" (last assistant msg only) + persistent "Summarize conversation → file" (`mode:"summarize"` synthesis, dedups vs filed Q&A; client appends a final user turn for the last-must-be-user gate). `AskAiDock` gets parity "File this answer". Shared `lib/briefcase/qa-item.ts` (`buildQaItem` → identical schema-valid `qa` items; unit-tested). `ProjectDetail` "Build deliverable" wired: template `<select>` → `POST /api/projects/[id]/build` → `/p/[id]` (was disabled "Coming soon"). MCP + `/api/b/*` + `/api/converse` UNTOUCHED. ⬜ **prod live-verify** (`root1_unify_live_verify`, due 2026-06-18): off-`/r/*` pill grounded-not-pitching, File+Summarize file items, Build builds & navigates; public `/welcome` still funnel-voiced. Follow-ups F-1/F-2/F-3 → `docs/superpowers/plans/2026-06-15-root-R1-followups-handoff.md`.
- [x] Free ZIP investor composite — **ZHVI ingest** (`ingest/{duckdb_pipelines,pipelines}/zhvi_swfl/**`, Tier-1→Tier-2 clone of ZORI; cadence + GHA day 22/23; verified live 33,922 rows/109 ZIPs) + **home-values-swfl** (per-ZIP `home_values_by_zip`) + **investor-zip-swfl** (value+rent+flood joined by exact key → gross rent yield → flood-adjusted cap rate, in code; STR null + ODD `airdna_str_swfl` parked). Fixed pre-existing `rentals-swfl` smoothing-lint time-bomb. 44 tests; vocab `--all` 30 brains. ⬜ **deploy:** run zhvi-tier1/2 workflows + `GRANT SELECT … data_lake` → real `.md`s on first nightly.
- [x] tier-divergence-swfl — the K-shaped market indicator (luxury-vs-starter ZHVI spread per ZIP; the price/segment axis pairing seller-stress's churn axis). BUILT + on `main`: Tier-1→Tier-2 ingest (`ingest/{duckdb_pipelines,pipelines}/tier_divergence_swfl/**`, FULL OUTER JOIN of top 0.67-1.0 / bottom 0.0-0.33 RAW tiers) + 2 views (`docs/sql/20260614_tier_divergence_views.sql`) + `tier-divergence-swfl` pack (+ `_tier-divergence-oracle.mts` + 2 tests) + 5 vocab slugs + cadence ×2 + 2 crons (day 21/22). Probe: 107/109 SWFL ZIPs both-tier → per-ZIP grain; median spread 2.51×. Polarity audited (rising luxury casts NO bullish vote). Catalog `KNOWN_INCOMPLETE`. Gates green (pack 20/20, vocab `--all` 0 orphans, fixture render = bearish). Spec `docs/superpowers/specs/2026-06-14-tier-divergence-swfl-design.md`. ⬜ **deploy:** run tier-divergence-tier1/2 workflows + apply the view SQL (`GRANT … data_lake`) → graduate to BRAIN_CATALOG on first clean cycle + recalibrate DEADBAND (`SOURCED.md#tier-divergence-swfl-deadband`).
- [x] Fixture-frames live (L0–L3): L0 binder seam ✅ · L1 storm-timeline ✅ · L2 franchise-survival ✅ · L3 seasonal-radial ✅ (on `main`)
- [x] Email Digest Phase 1 (`docs/superpowers/plans/2026-06-11-email-digest-phase1.md`) — ✅ @react-email + types.ts + log-io.mts + GHA cron; ✅ DigestEmail.tsx (Task 4, themeable via `BrandTheme`/white-label logo slot, 7 sections) + build-digest.mts (Task 6, delta floors/idempotency/RFC 8058); ✅ **fetch-digest-data.mts rebuilt vs the REAL brain schema** (was hallucinated → empty digest: brace-match OUTPUT parse, cell-based ZIP rows via `row.key`+`cells`, `housing_`-prefixed county slugs, markdown-table city voices); ✅ **city-voice curation** (EMAIL.md Rule 2.5: market-relevant ranking + dedup + `SUBJECT_TOPICS` allowlist so breaking never leads the subject + data-lede fallback `33908 DOM hits 87 | SWFL Data Gulf`); ✅ DIGEST_SENDER_NAME/ADDRESS/CONTACT set + Resend domain verified + 3-key strategy (digest-cron/waitlist-web=sending_access, full_access=Phase 2 Audiences). 32 email tests green; live dry-run renders 20KB from real data. ⬜ **runtime verify only:** first automated cron send (Mon–Fri 10:00 UTC) delivers + logs `send_status=sent` (check `digest_cron_first_send_verify`).
- [~] Email Digest Phase 2 — subscriber list (`docs/email-marketing/README.md` Phase 2). ✅ **capture path BUILT + live-verified 2026-06-12** (separate list from `waitlist`; Audiences→**Segments** SDK migration verified live): `public.email_subscribers` table applied; `lib/email/marketing-client.ts` (full_access client + `getDigestSegmentId`); `scripts/email/setup-digest-segment.mts` ran live (segment created); `app/api/email/subscribe` (idempotent contact-create → DB mirror, e2e 200 against real Resend+Supabase, test rows cleaned); `components/email/DigestSubscribe.tsx` wired into landing Footer + every `/r/[slug]`; `app/api/email/broadcast` (bearer, draft-by-default, rejects HTML w/o `{{{RESEND_UNSUBSCRIBE_URL}}}`). 8 new validation tests (40 email total) + tsc/eslint clean. ⬜ **go-live flip:** Vercel env (`RESEND_AUDIENCES_KEY`/`RESEND_DIGEST_SEGMENT_ID`/`DIGEST_BROADCAST_SECRET`) + redeploy; **CAN-SPAM address swap (hard gate)**; wire cron → broadcast (footer unsub token + POST to `/api/email/broadcast`).
- [~] Multi-tenant email — **Unit F cron scheduler** (`docs/superpowers/plans/2026-06-12-email-product-multitenant/`, Wave 2 capstone) BUILT + pushed (`63dbbf1`/`982c3d3`/`616dd3b`): `lib/email/scheduler.ts` (pure DI core) + `scripts/email/run-schedules.mts` (claim via `claim_due_email_schedules` RPC `FOR UPDATE SKIP LOCKED`; imported `computeNextRunAt` cadence/DST; usage-gate skip+notify; unsubscribe-token inject+assert; crash-orphan reaper; 30s broadcast timeout; DRY_RUN read-only) + `.github/workflows/email-scheduler.yml` (`*/15`). 149 `lib/email` tests; spec+code-quality reviewed. ⬜ **go-live:** apply `docs/sql/20260612_email_schedule_claim_fn.sql` (direct PG — was firewalled in build session) + set GHA secret `DIGEST_BROADCAST_SECRET` (missing; matches Vercel) + first live DRY_RUN→real verify. Check `email_scheduler_f_live_verify`.
- [x] Welcome chat → recurring-email hook (copy only, `233fc06`): `WELCOME_SYSTEM` + 4 PROMPTS + footer now lead with "auto-email fresh data to YOUR clients", not "sign up and you can build it"; route test re-pointed off the old funnel onto the no-invention + auto-email hook; 4/4 green.
- [~] Welcome trust UI — grounded ZIP answer as cited metric cards (`~/.claude/plans/update-and-plan-send-quizzical-stroustrup.md`). BUILT + verified LOCALLY: `lib/welcome/frames.ts` typed SSE contract (`{type:"place|data|text|done|error"}`, back-compat with the route's legacy un-typed frames) + pure reducer/formatters (28 tests); zero-dep shadcn-shaped `components/ui/{card,badge,skeleton,separator}` + `lib/utils.ts`; `app/welcome/_components/*` (ZIP hero + echo-back, skeleton→cards split reveal, citation chips, "Data as of" freshness badge, streamed-cursor synthesis); `GroundedAnswer`+`ConversationalChat` split (existing chat verbatim). MOAT guards on the card contract: per-metric `is_true_zip`+`coverage_label` (county figure can't read as ZIP) + `isInternalSource`/`citationLink` default-deny (no data_lake/supabase leak) + no-math. Demo path `app/api/welcome/demo` (`?demo=1`) replays the contract with real FMB values (33931 = $30,074/yr AAL) so it browser-smokes before the backend `{type:"data"}` lands; did NOT touch the parallel `chat/route.ts`. `safeLogoUrl` wired into `page.tsx` (completes the `welcome_logo_ssrf_allowlist` handoff). tsc 0 / eslint clean / live SSE smoke green. ✅ **backend producer BUILT 2026-06-13:** `app/api/welcome/chat/route.ts` grounded path emits `{type:"place"}`+`{type:"data"}` ahead of the prose stream via new `lib/welcome/answer.ts` — 3 cited hero cards (home value = Redfin median **sale price** `housing-swfl`; rent = ZORI `rentals-swfl` `rent_index_latest`; flood = FEMA per-ZIP AAL `env-swfl`). Gating (`is_true_zip`/`coverage_label`) rides from the dossier line, never relabeled; flood double-gated (explicit ZIP + true-ZIP, mirrors grounded.ts); sources default-deny (prettySource + drop internal URLs); per-ZIP value grain matches `is_true_zip` by construction. Permits HELD — its only headline is a corridor z-score (ratio), not a clean hero number (`welcome_permits_hero_card`). 7 unit + 13 route tests green; app tsc 0 / eslint clean. ⬜ manual browser paint smoke (operator); ⬜ prod live-verify (`welcome_live_cards_verify`).
- [~] Email funnel "the rest" (`docs/superpowers/plans/2026-06-13-email-funnel-the-rest/`) — 5 grounded tasks closing the gap from the built-but-OFF multi-tenant engine to the welcome promise: 01 `scope jsonb` column + parser capture (safe-additive) · 02 scoped content via the grounded engine · 03 worker go-live (existing check `email_scheduler_f_live_verify`) · 04 Stripe billing · 05 inbound reply-to-send (← now built as the Buyer-Intent Reply Sensor below). Checks: `email_scope_column`/`email_scoped_content`/`email_stripe_billing`/`email_inbound_reply`. ✅ **02 scoped content BUILT 2026-06-14** (`lib/email/scoped-content.ts` resolveScope/assembleScopedContent/renderScopedBody + 03b `buildContent` branch+cache in `run-schedules.mts`; cards from `buildWelcomeAnswer` only, no-invention; `place_label` + topic-gated subject; 25 scoped tests, DRY_RUN-verified "Cape Coral market — this week"). Remaining: 03 worker go-live · 04 Stripe · 05 (built as Reply Sensor).
- [~] "It's Alive" activation-delta sequence (growth #6; `docs/superpowers/specs/2026-06-13-activation-delta-sequence-design.md`) — opt-in → cited ZIP report (email#1) → +3d same report with WHAT CHANGED (email#2) → CTA to gate. **Phase A+B BUILT + tested** (`lib/email/activation/*`, 31 tests; `email-report.html`; `DigestSubscribe` additive `activation` mode; `/api/email/subscribe` stores consent/scope/`prospect_brand`; migration `docs/sql/20260613_activation_sequence.sql` NOT YET RUN). Delta engine diffs the distilled dossier ("what we showed you") — never invents a change. **Phase C scaffolded DRY_RUN-only** (`scripts/email/run-activation.mts` + `activation-sequence.yml`, workflow_dispatch, no cron). ⬜ **Phase D go-live (gated):** swap CAN-SPAM address, apply migration + set secrets, choose+verify the 1:1 send (Broadcast segment-of-one vs transactional+List-Unsubscribe — the `{{{RESEND_UNSUBSCRIBE_URL}}}` tag is Broadcast-only), THEN flip cron. Hard-gated on `city_pulse_supersession` (due 2026-06-15). Checks: `email_activation_consent`/`prospect_snapshot_store`/`report_delta_computer`/`email_report_renderer`/`activation_sequence_golive`.
- [~] **Buyer-Intent Reply Sensor** (implements + supersedes task-05 `email_inbound_reply`) — the email product reframed: reply-to becomes a warm-lead detector, not a newsletter. CODE COMPLETE this session: token addressing (`lib/email/reply-token.ts`) + per-send persistence (`docs/sql/20260613_email_sends.sql`, scheduler `resolveReplyTo`/`recordSend` optional deps wired in `run-schedules.mts`); shared grounded core extracted (`lib/grounded-answer.ts`, converse refactored to it, golden-snapshot pinned); inbound webhook (`app/api/webhooks/resend/route.ts`) w/ Svix HMAC verify (`lib/email/svix-verify.ts`, no new dep) → `resend.emails.receiving.get` body fetch (SDK-verified) → DI orchestrator (`lib/email/process-inbound.ts`); ordered auto-reply gates (identity→loop→throttle→thread→breaker, `lib/email/inbound-guards.ts`); agent alert + `/alerts` dashboard (`docs/sql/20260613_buyer_intent_events.sql`). ~270 tests green, tsc/eslint clean. ⬜ **runtime go-live (no creds in build container):** apply the 2 migrations (direct PG); add Cloudflare **MX for `reply.swfldatagulf.com`** (value from Resend) + verify; set Vercel `RESEND_WEBHOOK_SECRET` + `REPLY_DOMAIN` + point the Resend inbound webhook at `/api/webhooks/resend`; **Step-0 empirical proof** (non-test send → confirm `Reply-To: r-{token}@…` in delivered raw headers) before relying on the broadcast path; live e2e (known→cited auto-reply, unknown→alert-only). Checks: `reply_domain_mx_live`/`resend_webhook_sig_verified_live`/`inbound_parse_e2e`/`autoreply_guard_live`/`alerts_rls_live_verify`.
- [~] MCP co-build tools — `swfl_project_list/add/build` write tools; service-role write hard-bound to key's project (S9). Code COMPLETE locally (`9cdc302`, `eca9c7c`): per-project `mcp_key` mint/regenerate=revoke + "Connect your AI" UI; `X-Project-Key` header ONLY (no arg fallback — vendor-verified the SDK forwards the header to the tool `extra`; key is structurally absent from input schemas = never in tool-call logs); `[LB-R6b]` write hard-bound to the key's project (smuggled `project_id` ignored — negative test); `origin:'mcp'`; dedupe; shared `lib/deliverable/assemble.ts`. 28 MCP tests green, tsc/eslint clean. **HELD on two operator gates before ship: (a) `MCP_BEARER_TOKEN` SET in prod (keystone — server is unauthenticated until then), (b) live MCP-surface diff-review.** Live two-Claude verify + `mcp_project_tools_live_verify` close are post-deploy.
- [~] Pivoted Views execution (`docs/superpowers/plans/2026-06-12-pivoted-views-build/`) — **spine §01–07 SHIPPED + cut over.** §02 `zhvi_pivoted`(316)/`zhvi_zip_latest`(109) + §06 `zori_pivoted`(136)/`zori_zip_latest`(94) views LIVE; §03 `/charts` (ZHVI) live; §04 **GATE A cycle 3/3 verified** (`426df6e`); §05 **GATE B cutover LIVE** — packs now read the views (`home-values-swfl.mts:553` / `rentals-swfl.mts:555`), cutover `e29d21d`, partial-view floor `72465f0` (ZHVI 90 / ZORI 79); §07 freshness liveness (ZHVI wired; **ZORI `liveness_view` registered 2026-06-13**); §99 slug coverage GREEN (vocab `--all` exit 0, slugs held). ✅ **`zori_pivoted` connected to `/charts`** (generic `MetroAreaChart`, second stacked rent panel; `PANELS[]`-driven for future charts). ✅ §08 `view_vintages` (GREENLIT 2026-06-13) **§08a+b BUILT** — migration `20260612_view_vintages.sql` applied (empty append-only table) + `capture_view_vintages.py` (generic unpivot + `::float8` R1 tripwire) + `SourceTag "view_vintage"` + UNWIRED reader `view-vintage-reader.mts`; cron `view-vintages-monthly.yml` **day 26** (Firecrawl-verified Zillow cadence). ⬜ §08b first capture = day-26 cron (PIT clock start → check `view_vintages_first_capture`); ⬜ §08c EXCLUDED→BACKTESTABLE flip DEFERRED ~9mo (→ check `view_vintages_backtestable_flip`). ✅ **buildSnapshot retired** to test-only `_home-values-oracle.mts`/`_rentals-oracle.mts`, 6 importers re-pointed (the "9" was a generic-name miscount), OUTPUT byte-stable.
- [~] franchise-outcomes real data — SBA 7(a) FOIA Tier-1 DuckDB pipeline BUILT + pushed (`e6f68e2`/`1df16dc`): `ingest/duckdb_pipelines/franchise_outcomes/` downloads 3 FOIA CSVs → 453 Lee+Collier franchise rows → county-grain + ZIP-approx Parquets in `s3://lake-tier1/franchise/`; `get_zip_approx()` enrichment; GHA cron `franchise-outcomes-quarterly.yml` (15th Jan/Apr/Jul/Oct 08:00 UTC); `cadence_registry` parked `not_yet_running:`; `REFINERY_FRANCHISE_SOURCE=live` fixture swap gate wired in `franchise-source.mts`. ⬜ **flip to live:** first quarterly cron run lands ≥50 real brands in Parquet → set `REFINERY_FRANCHISE_SOURCE=live` + rebuild brain → close `franchise_foia_first_run` check. Phase 2 (direction vote + ZIP detail_tables consumer) deferred; polarity table documented in `SOURCED.md#sba-foia-franchise-row-counts`.
- [~] **lee_permits Firecrawl→crawl4ai cutover** (`docs/superpowers/plans/2026-06-16-crawl4ai-accela-port.md`) — Firecrawl is GONE; the 3 Accela pipelines (`lee_permits`/`collier_permits`/`dbpr_sirs`) were dead. Ported `lee_permits` to **crawl4ai 0.8.9 + UndetectedAdapter**: new `ingest/lib/crawl4ai_client.py` (`Crawl4aiSession` reused-browser + `fetch_many` parallel) + `scraper.py` rewrite (paginated session loop, defined-marker first-row pagination, parallel CapDetail enrich w/ URL-absolutize, restored silent-wrong-date guard) + `requirements.txt` pin + `scraped_via=crawl4ai`. Also fixed a **pre-existing ZIP-MOAT bug** (`_extract_zip` stored the 5-digit house number as `zip_code`). Verified LIVE (home IP): 91 rows/11 pages, dates exact, enrich 5/5; 24 unit+parser tests green; decision-gate spike 4/4. ✅ **GHA datacenter-IP RESOLVED** — dry-run probe on a GHA runner (run 27602909470, 1m59s) cleared the WAF, 91 rows/11 pages, patchright+playwright chromium install works; cron CAN run on GHA, no self-hosted/proxy needed (`crawl4ai_accela_gha_ip` CLOSED). ⬜ **ONLY GATE LEFT:** operator approval for the first prod lake write (`lee_permits_first_lake_ingestion`) → uncomment the `schedule:` in `lee-permits-weekly.yml` (Monday cron, real dlt merge) OR one manual `-f dry_run=false`. Then `collier_permits`/`dbpr_sirs` are one-`.step()` follow-ons.
- [x] Killed the per-ZIP $39/$79 "Order this report" paywall on `/r/zip-report/[zip]` (operator: no one pays per-ZIP) → free `DigestSubscribe` email capture (`source="zip-report"`). Paid path stays the bearer-gate + higher-value surface, not a per-ZIP page (check `paid_path_wtp`).
- [x] **SOLO-24 — Freshness as an SLA (per-source `warn_after`/`error_after`)** (`docs/audit/2026-06-21-full-platform-audit/PLAN/phase-7-best-practices-hardening/data-reliability/SOLO-24-freshness-error-sla--SONNET.md`) — `check_freshness.py` now exits 1 when an opted-in source breaches `error_after_days`; `warn_after_days` logs 🟡 SLA WARN (exit 0). Three pilot entries in `cadence_registry.yaml`: `live_search_daily_median_price` (1d; warn≥2, error≥4), `city_pulse` (1d; warn≥4, error≥7), `leepa` (365d; warn≥540, error≥730). `check_sla_violations` + `format_sla_section` added; `freshness_sla` carried from each registry entry through `run_probe`. `--sla-dry-run` flag suppresses exit 1 for local testing. DB-connection/probe-error paths stay exit 0 (build 03 invariant unchanged). 6 new unit tests in `ingest/tests/scripts/test_check_freshness_sla.py`; all 17 (new + existing) green.
- [x] `/ops/data-inventory` — internal ZIP grain + cadence inventory page. All 48 pipelines organized by schedule (daily→weekly→monthly→quarterly→annual), non-ZIP grain first in each section. Per-section "Mark ZIP Grain Routing Done" action panel (localStorage-persisted notes). Excel-style dark table with grain/status/ZIP badges. Identifies 30+ datasets missing ZIP grain and what it would take to add it.
- [x] **Deliverable Convergence — Task 7 (build→schedule bridge)** (`docs/superpowers/plans/2026-06-16-deliverable-convergence/`, Wave C, blocks Tasks 5/6). Turning a built `"email"` deliverable into a weekly send copies its **recipe** (template + ZIP scope + audience + cadence), never the frozen snapshot. 3 tested units + 1 route wiring: `lib/deliverable/schedule-recipe.ts` (`deliverableToScheduleRecipe` → `ParsedCommand`, `template_id:"report"`, no snapshot/narrative/topic) · `lib/email/schedule-signature.ts` (9-col signature + `IS NOT DISTINCT FROM` filters) · `lib/email/schedule-upsert.ts` (`createOrTouchSchedule` NULL-equal find-or-update; a match in ANY status — incl. stopped — reactivates in place like un-pausing; one row per recipe) · `app/api/email/schedule-command/route.ts` (no-LLM `fromDeliverable` propose branch + universal idempotent create). 23 new tests; `bun test lib/email lib/deliverable` 577/0; tsc/eslint clean. ⬛ **Live-verify gated** on `docs/sql/20260616_deliverables_scope.sql` apply (shared w/ Task 4) → check `build_to_schedule_bridge` (due 06-20).
