# 2026-05-25 — MarketBeat Flow 3 shipped + branch hygiene

## Context

Continuation of the firecrawl-pipeline-skeleton arc. PR #15 (commit `b2fa78e`) shipped the brain-side scaffold for `data_lake.marketbeat_swfl` + `corridor_profiles.character_broker_narrative`. PR #17 (commit `93b778b`) pivoted the operational layer off n8n onto GitHub Actions `workflow_dispatch` Firecrawl-py crons. Commit `73b96ce` shipped the 26-corridor alias table (`refinery/lib/marketbeat-submarket-aliases.mts`) as pre-flight for the per-submarket join. This session wired that join into the CRE pipeline end-to-end and merged.

## Decided

- **PR #18 (commit `f815c58`) squash-merged to main 2026-05-25.** Title: _"feat(cre): MarketBeat Flow 3 — per-submarket key_metrics in cre-swfl."_ Auto-PR workflow opened the PR on push; body was replaced with the structured summary post-hoc. All checks (open-pr, Vercel, Vercel Preview Comments) SUCCESS. Branch `feat/marketbeat-flow3-per-submarket` deleted both locally and on remote.
- **New helper `submarketSlug` in `refinery/lib/marketbeat-submarket-aliases.mts:N`** + 4 unit tests (idempotency on 6 known submarkets, collision smoke test walking `MARKETBEAT_SUBMARKET_MAP` keys, double-space edge case documented as known lossy collapse). 12 → 16 tests.
- **New pure join `groupCorridorsBySubmarket` in `refinery/sources/cre-source.mts`** returning `Map<rawSubmarket, JoinedSubmarketGroup>` keyed on the **raw** submarket string ("Fort Myers"), not the slug. Producer calls `submarketSlug(group.submarket)` when building metric keys. 6 new unit tests. 17 → 23 tests.
- **`cre-swfl` producer emits up to 9 new keys per run** (3 submarkets × `{vacancy_rate, asking_rent_nnn, absorption_sqft}`) gated on non-null source values. Schema pinned per family: vacancy_rate (intensive/percent/percent), asking_rent_nnn (intensive/USD/sqft/currency), absorption_sqft (extensive/sqft/count).
- **Citations use `encodeURIComponent(group.submarket)`** for multi-word submarkets — `Fort Myers` → `Fort%20Myers` — so Supabase PostgREST URLs don't silently zero-result. Naples was the only single-word submarket that escaped clean.
- **Three caveat types added:** per-family rollup-direction (dynamic slug list, one caveat per emitting family), zero-matched-corridors guard, unmatched-corridors disclosure listing corridors whose alias maps undefined OR resolved to a submarket without an mbRow this run.
- **`cre-swfl-singleton-reset` backlog closed.** Reset block at top of `creCorpusSummary` zeroes all 5 module stashes (`lastCorridors`, `lastCorridorFetchedAt`, `lastPermitsSwflOutput`, `lastMarketbeatRows`, `lastMarketbeatFetchedAt`) + the new `lastJoinedBySubmarket`. Regression test runs `creCorpusSummary` twice in-process with different fragment sets and asserts no Fort Myers bleed from run 1 into run 2.
- **New CLI flag `--target-only` in `refinery/cli.mts:29`.** Rebuilds ONLY the named pack; every upstream is logged but skipped regardless of fresh/stale/missing. Mutually exclusive with `--force`. Built specifically to keep fixture-mode verification from touching sibling brain artifacts — happened mid-session when `--force` downgraded `brains/storm-history-swfl.md` + `brains/permits-swfl.md` from live data to fixture data. CT restored those via `git checkout --` and added the scoped flag in the same PR.
- **Operational status of cre-swfl per-submarket metrics:** code path is live but emits 0 keys until `data_lake.marketbeat_swfl` has verified rows. With production table empty, `lastJoinedBySubmarket.matched.size === 0` and the OUTPUT block is identical to today's. Trigger: human runs the MarketBeat GitHub Actions `workflow_dispatch`, flips `verified=true` on inserted rows, next `npm run refinery cre-swfl` rebuild lights up the 9 new keys.
- **Spec validator is structural-only.** Verified that emitting `absorption_sqft_marketbeat_naples` without prior SKOS registration is safe for the validator gate — `validateSpec` does not cross-check metric names against any concept registry. The new keys will surface in `docs/orphan-triage.md` as orphans (not blocking) until the SKOS PR ships.
- **Test totals:** 637/637 bun tests green (622 baseline + 15 new). `npx tsc --noEmit` clean.
- **`gh` CLI confirmed installed locally** at `C:\Program Files\GitHub CLI\gh.exe`, authenticated as `ethanrickyjrjr-wq` with `gist`, `read:org`, `repo`, `workflow` scopes. Not on the active PowerShell session's PATH — a `pwsh` restart fixes that. winget reports "already installed, no upgrade available." This is durable; future PR creation can use `gh pr create` directly.

## Open

- **SKOS `raw_slug_patterns` registration is the next follow-up.** Three new metric families need concept entries in `refinery/vocab/brain-vocabulary.json` so master / cross-brain consumers can resolve the new slugs: `vacancy_rate_marketbeat_*`, `asking_rent_nnn_marketbeat_*`, `absorption_sqft_marketbeat_*`. **Hard rule for the follow-up:** keep `asking_rent_psf` (corridor-level, PSF, no NNN qualifier) and `asking_rent_nnn` (MarketBeat, NNN-qualified) on DISTINCT concepts. They are different sources with different denominators (corridor editorial vs broker survey). Same pattern env-swfl uses for `swfl_zip_{ZIP}_*` — see [[per-zip-vocab-design]].
- **Stale local feature branches** — `feat/bls-laus-county-ingest` (merged via PR #14, commit `b9fa5f9`) and `feat/firecrawl-pipelines-github-actions` (merged via PR #17, commit `93b778b`). Both safe to delete locally. Not deleted in this session because the user only asked for merge-and-sync, not branch pruning. Backlog: `git branch -d feat/bls-laus-county-ingest feat/firecrawl-pipelines-github-actions`.
- **Stale remote tracking refs** — `origin/feat/firecrawl-pipelines`, `origin/feat/firecrawl-pipelines-github-actions`, `origin/feat/marketbeat-flow3-per-submarket` (now gone on origin since the merge). Run `git remote prune origin` next session to clean up.
- **MarketBeat GA `workflow_dispatch` ingest run** — not yet triggered. When run, requires a human to eyeball MarketBeat rows and flip `verified=true` before the cre-swfl rebuild will pick them up. The Tier-2 brain-first gate is satisfied (consuming brain ships in same PR family); operational trigger is the gating step now.
- **Per-corridor `character_render` enrichment + per-corridor synthesis facts** citing the broker MarketBeat read — explicitly deferred. Out of bounded scope for this PR.
- **QoQ direction computation for per-submarket metrics** — needs historical `marketbeat_swfl` rows; today's `selectLatestVerifiedPerSubmarket` keeps only latest per submarket. Backlog: relax the upstream pick OR retain prior quarter in a sidecar. Direction ships as `"stable"` schema-required fallback until then.
- **Fixture name-drift cleanup** — `refinery/__fixtures__/corridor-profiles.sample.json` has names that don't exactly match the alias table: `"Estero Blvd / Fort Myers Beach"` vs alias `"Estero Blvd Fort Myers Beach"`; `"Gulf Coast Town Center / Alico Rd"` vs alias `"Gulf Coast Town Center"`. These currently land in the `unmatched` bucket. Reconciling against `fixtures/corridor-rents.json` (alias's source of truth) is its own cleanup PR.
- **Phase out SWFL-wide medians** when downstream consumers (especially master synthesis) migrate to the per-submarket break-outs. Backlog only; not blocking.

## Verify next time

- `git log --oneline -5` against this note. `f815c58` should be the most-recent commit and the Flow 3 ship; nothing past it.
- `git remote prune origin` is safe to run — won't delete anything that's still on remote.
- `gh pr list --state merged --limit 5` to confirm PR #18 is the most recent merge (after #17, #16, #15, #14).
- `bun test` should be 637/637 green on a fresh checkout.
- `npm run refinery cre-swfl` against the empty production `marketbeat_swfl` table should produce a diff-clean `brains/cre-swfl.md` — zero `*_marketbeat_*` keys, identical OUTPUT to pre-Flow-3 state. That's the non-regression check before trusting the next ingest run.
- Before scoping the SKOS follow-up: read [[per-zip-vocab-design]] to confirm `raw_slug_patterns` is still the right hook + that `asking_rent_psf` and `asking_rent_nnn` are on distinct concepts (don't collapse).

## Mid-session redirect worth recording

CT ran `npm run refinery cre-swfl` for fixture-mode verification using `--force`, which bypassed the fresh-skip check and regenerated TWO unrelated upstreams (`storm-history-swfl.md`, `permits-swfl.md`) from fixture data, downgrading them from live. User reaction was sharp: _"what the fuck are we doing???"_ and an explicit directive to (a) `git checkout --` the unrelated brains immediately to keep the PR clean, and (b) fix the root cause in the same PR — _"add a scoped rebuild flag as a follow-up backlog item, OR just fix it in this same PR if it's a one-liner."_ CT opted (b) and shipped `--target-only` in the same PR. This is the right pattern for the future: when fixture-mode verification touches sibling artifacts, the root cause is the rebuild command, not the artifacts. Don't paper over it with `git checkout` alone.
