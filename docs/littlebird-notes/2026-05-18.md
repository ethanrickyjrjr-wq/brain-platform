# 2026-05-18 — Seed session (historical, pre-rebrand)

> **Preserved as historical context — 2026-05-26.** At the time this note was written, the project's working brand was "Plumbline" and Notion access was scoped to the other project (Premise). On 2026-05-26 the project locked to **SWFL Data Gulf** and the Notion sync target moved to the "SWFL Data Gulf" page (id `35135f3b7faf8151ba96c86fc8513e80`). The body below is left as-was so the historical thread is intact; current LB framing lives in `README.md`.

## Context

First entry. CT was asked to rewrite LB's memory blob to be project-only — strip the Premise mindset, drop competitor name-checks, drop UI framing. The rewritten memory was delivered in-chat for paste-back into wherever LB boots (Notion-on-the-other-project at the time, since this project had no Notion access yet).

This directory (`docs/littlebird-notes/`) was created so LB has a place to record session notes inside the repo until Notion access was sorted.

## Decided

- LB's scope: **this project only**. No cross-project work.
- LB's lanes: strategy, research, plan review, catching CT drift, note-taking. CT codes; LB advises.
- Notes live at `docs/littlebird-notes/YYYY-MM-DD.md`, one file per session, append-only.
- Working brand name at this date was "Plumbline" — **not locked**, see Open. Internal code-side used "Brains" / "Master Brain"; no rename work scheduled at the time. (Brand locked to **SWFL Data Gulf** on 2026-05-26.)
- Tier 1 Parquet policy supersedes any earlier "tabular stays in Postgres" framing — keep this top-of-mind when reviewing ingest plans.

## Open (as-of-date 2026-05-18)

- **Brand name unresolved.** Strict WHOIS check found "Plumbline" taken on every primary TLD (.com/.app/.io/.co); "Keystone" (the prior fallback) also taken. Working name in copy/docs but cannot be treated as locked. _Resolved 2026-05-26 — locked to **SWFL Data Gulf**._
- **Notion access for this project.** Scoped to the other project at the time; mark to track. _Resolved — `NOTION_API_KEY` + `NOTION_LITTLEBIRD_PAGE_ID` wired in `.env.local`; `roadmap:sync` pushes to the "SWFL Data Gulf" page._
- **Where does LB's system prompt actually live?** Need to confirm Notion page vs. Craft doc vs. fresh-paste each session. _Still open — the install location remains Ricky's call._
- **Cosmic Rolling Brook v2 wave structure.** v1 plan scope was stale at this date (Wave 0 geometry DDL + Wave 4 brand-in-React/Sanity killed in v2). _Resolved — v2 shipped, see `docs/sessions/cosmic-rolling-brook-v2-shipped.md`._

## Verify next time

- That the rewritten memory blob was actually pasted into LB's boot context — not just acknowledged in chat.
- Latest `git log --oneline -10` against the "Shipped" list in LB's memory — anything past commit `1049f77` needs to be promoted into memory.
- Current Cosmic Rolling Brook v2 plan source before trusting any wave-number reference.

## Memory-blob drift noted at the time

The rewritten memory blob delivered in chat that day carried v1-plan drift the user caught:

- Cosmic Rolling Brook section referenced Waves 0 / 1 / 2 / 3 / 4 / 5; Wave 0 and Wave 4 were dead in v2.
- "User-facing brand: **Plumbline** (locked)" line was wrong — should have read "working name, unresolved."
- "Wave 4: Plumbline branding + commercial UX" bullet was tied to a wave that didn't exist anymore.

_All three of these are moot now — the brand is **SWFL Data Gulf** (locked 2026-05-26), and Cosmic Rolling Brook v2 has shipped._
