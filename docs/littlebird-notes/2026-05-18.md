# 2026-05-18 — Seed session

## Context

First entry. Big Bird asked CT to rewrite LB's memory blob to be Plumbline-only — strip the Premise mindset, drop competitor name-checks, drop UI framing. The rewritten memory was delivered in-chat for paste-back into wherever LB boots (Notion-on-the-other-project today, TBD here).

This directory (`docs/littlebird-notes/`) was created so LB has a place to record session notes inside the Plumbline repo until Notion access is sorted on this project.

## Decided

- LB's scope: **Plumbline only**. No cross-project work.
- LB's lanes: strategy, research, plan review, catching CT drift, note-taking. CT codes; LB advises.
- Notes live at `docs/littlebird-notes/YYYY-MM-DD.md`, one file per session, append-only.
- Working brand name is **Plumbline**, but it is **not locked** — see Open. Internal code-side still uses "Brains" / "Master Brain"; no rename work scheduled.
- Tier 1 Parquet policy supersedes any earlier "tabular stays in Postgres" framing — keep this top-of-mind when reviewing ingest plans.

## Open

- **Brand name unresolved.** Last session's strict WHOIS check found Plumbline taken on every primary TLD (.com/.app/.io/.co); Keystone (the prior fallback) also taken. "Plumbline" is the current working name in copy and docs but cannot be treated as locked. Need: a candidate list that survives WHOIS + USPTO + handle-availability before any rename work is scoped.
- **Notion access for Plumbline.** Currently scoped to the other project. Big Bird said "we'll deal with that next" — track until resolved. Until then, these markdown notes are the system of record.
- **Where does LB's system prompt actually live today?** Need to confirm whether it's a Notion page, a Craft doc, or pasted into a fresh chat each time. The rewrite has been delivered as text; the install location is Big Bird's call.
- **Cosmic Rolling Brook v2 wave structure.** v1 plan scope is stale — Wave 0 (geometry DDL dump) and Wave 4 (Plumbline rename inside React/Sanity) were killed in v2. Need the current v2 wave list before reviewing any plan that cites wave numbers.

## Verify next time

- That the rewritten memory blob was actually pasted into LB's boot context — not just acknowledged in chat. (And that it was updated to drop the stale Wave 0 / Wave 4 references + the "Plumbline locked" framing — see "Memory-blob drift" below.)
- Latest `git log --oneline -10` against the "Shipped" list in LB's memory — anything past commit `1049f77` needs to be promoted into memory.
- Current Cosmic Rolling Brook v2 plan source before trusting any wave-number reference.

## Memory-blob drift to fix

The rewritten memory blob delivered in chat today carries the same v1-plan drift the user caught in this note:

- Cosmic Rolling Brook section names Waves 0 / 1 / 2 / 3 / 4 / 5 — at minimum Wave 0 and Wave 4 are dead in v2. Whole "Cosmic Rolling Brook" subsection needs to be rewritten against v2 or stripped until v2 is sourced.
- "User-facing brand: **Plumbline** (locked)" line is wrong — see Open above. Replace with "working name, unresolved pending WHOIS-clean candidate." Remove the "Domain TBD" parenthetical that names .com/.app/.io as if they're still on the table.
- "Wave 4: Plumbline branding + commercial UX" bullet inside the Cosmic Rolling Brook block needs to go with the wave-list rewrite.

Action: when LB's memory is next installed, apply those three edits before paste-in. Don't ship the blob as-delivered.
