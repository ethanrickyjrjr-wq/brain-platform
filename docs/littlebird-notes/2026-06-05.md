# MHS Extraction Architecture — 2026-06-05

> Session log dump (2026-06-05, 14:27 EDT). Source PDF: `drop/mhs-market-trends-2026.pdf`
> (Maxwell Hendry Simmons CRE report). Geometry probe: `_build_geometry.py` (PyMuPDF installed).
> Written by CT at Ricky's direction; LB read-only.

---

## Decided

### MHS extraction = THREE recipes, not one

The document is three distinct schemas. Do not collapse into one extractor.

**RECIPE 1 — CRE-submarket** (Retail + Industrial + Office, same schema ×3)

- **Grain:** submarket rows (Bonita Springs, Cape Coral, Charlotte County, etc.)
- **Absorption period:** "Prior 12 Months" rolling window ending at report date.
  Stamp as `prior_12mo_ending=<report_date>`. **NOT** the page title year (QTD 2026).
  **NOT** the CoStar county block year (2025). Those are separate periods.
- **CoStar county block:** labeled "2025" in the header — full calendar year 2025.
  Different grain (county), different period. Extract separately or skip for now.
- **Geometry header-match is the ONLY valid absorption extraction path.**
  No counting-rule fallback anywhere. The counting rule was grabbing Under Construction.
  Proof: Cape Coral Under Construction = 56,248 (col adjacent, black, no parens).
  Absorption = (33,845) (red, parens, 7th column per Copilot eyes-on-PDF).
- **Dual-signal negative** (BOTH must be wired independently):
  - a. PAREN path: `r"^\([\d,]+\)$"` → strip parens, parse, negate. Fires first.
  - b. COLOR path: span color near (1,0,0) RGB float with tolerance → if parsed value
    positive, negate.
  - No double-negation. Once negative, stop.
  - `[COLOR-NEG]` log line required when color path fires without paren path
    (canary for future quarters).
  - Applies to absorption ONLY. Not vacancy. Not rent.
- **Red+parens = negative is document-wide** for ALL THREE sectors (retail, industrial, office).
  Confirmed by Copilot eyes-on-PDF for Retail. Treat same for Industrial/Office until
  Copilot check returns (pending — do not flip `verified=true` on Industrial/Office absorption yet).
- **Lely Industrial absorption = 0.** Geometry-confirmed. Not 16,941 (hand-key error, retired).

**RECIPE 2 — Permits**

- **Grain:** individual permit rows grouped by jurisdiction.
- **Jurisdictions are NOT submarket slugs.** Build a SEPARATE jurisdiction→canonical-place crosswalk.
  Known jurisdictions: Unincorporated Lee County, City of Cape Coral, Unincorporated Collier,
  City of Naples, City of Marco Island, Unincorporated Charlotte County, City of Punta Gorda,
  Town of Fort Myers Beach (confirmed present in older report pages).
  "Unincorporated Lee County" ≠ any CRE submarket. Do NOT reuse submarket slug table.
- **Period:** page title says "ISSUED PERMITS 2025". Exact date range TBD (Copilot check pending).
  Dates in visible pages span January–July. Stamp with `calendar_year=2025` until confirmed.
- **Schema:** Date / Asset Class / Project Address / Project Name / Permit Value / Building SF

**RECIPE 3 — Multi-Family**

- **Grain:** county (Lee, Collier, Charlotte) — NOT submarket.
- Two sub-sections: Notable Transactions (address-level) and Market Metrics (county summary).
- Market Metrics columns: Asking Rent Per Unit, YoY Rent Growth, Inventory (Units), Vacancy,
  Units Under Construction.
- **Period:** page title says "QTD 2026". Exact period TBD (Copilot check pending).
- Consuming brain: confirm `rentals-swfl` is the intended consumer before building this recipe.

### Build sequence (ship in this order)

1. **CRE-submarket recipe** (Retail+Industrial+Office, one recipe) → `cre-swfl` (already wired).
   Brain must be in same PR. Do not ship ingest without the consuming brain.
2. **Permits recipe** → `permits-swfl` (own jurisdiction crosswalk, own PR).
3. **Multi-Family recipe** → `rentals-swfl` (confirm consumer first, own PR).

---

## Open

### Before CRE-submarket write

- **A. Bonita Springs regression — CONFIRMED PARSER BUG.**
  Present in all three sectors per Copilot: Retail=57,648, Industrial=11,510, Office=(60,296).
  NOT a county aggregate. Find where the sector-detection rewrite dropped it and restore.
  Expected row count after fix: 15/sector minimum (Charlotte County slug decision still open).
  **→ First task this session.**
- **B. Charlotte County — present as normal submarket row in all three sectors.**
  Currently going to unmapped by slug design. Decision needed: canonical slug or deliberate exclude.
  Do not auto-decide. **Flag to Ricky.**
- **C. Industrial + Office Copilot visual check — PENDING.** Do not flip `verified=true` on
  Industrial or Office absorption until that check returns. Vacancy + rent
  (42/42 geometry-confirmed) are clean and ready to write regardless.
- **D. Period stamping — exact report date (`prior_12mo_ending` value) pending Copilot check.**
  Do not hardcode "2026-03-31" — confirm from PDF cover page or footer.
- **E. Backfill validation — before enabling decade backfill, test geometry header-match against
  oldest available historical PDF.** MHS may have used "Net Absorption" or split headers in
  earlier templates. Validate extremes of date range before declaring recipe stable.

### For Permits + MF (do not build yet)

- **F. Permits exact date range** (is "2025" full calendar year or partial?) — Copilot check pending.
- **G. Multi-Family period** ("QTD 2026" = what quarter/date range?) — Copilot check pending.
- **H. Market Rents + Vacancy in submarket tables:** are these point-in-time current or
  period-labeled? Need confirmed before stamping those fields.

---

## Verify next time

- **ODD pivot check (Standing Duty #6).** This is a multi-year PDF feed. Before enabling backfill:
  - Confirm consuming brains are ODD-safe.
  - Do not hard-code bucket counts as completion criteria.
  - Any slug whose classification is cadence-driven (not inherent) must be reviewed before
    the quarterly feed activates.
- Copilot eyes-on-PDF returns for: Industrial/Office absorption red+parens (item C),
  report date (item D), permits date range (item F), MF period (item G), submarket
  rents/vacancy point-in-time status (item H).
- Resolve Charlotte County slug decision with Ricky (item B) before any rows are written.

---

## Session Note

Per Ricky's directive: log updated first, no code touched until this file written.
Next action after confirmation: **Bonita Springs regression (item A)** — locate where the
sector-detection rewrite dropped the row; report findings. **No rows written** until Bonita
Springs is located and Charlotte County decision is flagged to Ricky.

---

# Copilot Visual Check Results — 2026-06-05, 14:43 EDT

> Appended after Copilot eyes-on-PDF + text extraction pass. Supersedes the prior Open-items
> list above where they conflict (see "Updated Open Items"). The period-stamping decision is
> now the blocking item.

## Decided (this pass)

### Schema consistency — LOCKED

Absorption = **7th column** in Retail, Industrial, AND Office submarket tables. Identical column
structure across all three sectors. One geometry recipe (header-match → col7) handles all three.
**No sector-specific column logic needed.**

### Industrial absorption (Copilot text extraction, parens confirmed)

| submarket                     | absorption | sign                                |
| ----------------------------- | ---------- | ----------------------------------- |
| Fort Myers (City of Ft Myers) | (202,228)  | negative, parens ✓                  |
| Lely                          | 0          | numeral "0" (not blank, not dash) ✓ |
| Bonita Springs                | 11,510     | positive, present ✓                 |
| Cape Coral                    | 45,339     | positive ✓                          |
| Charlotte County              | 211,532    | positive ✓                          |

### Office absorption (Copilot text extraction, parens confirmed)

| submarket                     | absorption | sign               |
| ----------------------------- | ---------- | ------------------ |
| Fort Myers (City of Ft Myers) | (142,622)  | negative, parens ✓ |
| Bonita Springs                | (60,296)   | negative, parens ✓ |
| Lehigh                        | (2,729)    | negative, parens ✓ |
| Lely                          | (8,736)    | negative, parens ✓ |
| Charlotte County              | 16,420     | positive ✓         |

### Color signal status

- **Retail:** Copilot visual read confirmed red = parens = negative document-wide.
- **Industrial + Office:** Copilot ran text extraction (no color metadata). Paren path catches
  100% of confirmed negatives in both sectors. Color path is **defensive/canary-only** for future
  quarters where format may change. **NOT blocking the write.**

### County block note

CoStar county block uses **"2025"** header (not "Full Year 2025", not "Calendar Year 2025" — just
"2025"). County absorption and submarket "Prior 12 Months" absorption are **TWO SEPARATE numbers**,
different grain, different period. County block negatives also use parens (e.g., Lee Industrial
(191,473)). **Dual-signal logic applies to county block too.**

### Permits

Full calendar year 2025 confirmed. Dates span 01/xx through 12/xx (last entries at 12/19, 12/23).
No explicit "January 1 – December 31, 2025" printed — just "2025" as section header.
Stamp as `calendar_year=2025`. **"Town of Fort Myers Beach"** jurisdiction confirmed present with
real permit rows (Estero Blvd retail). Needs its own slug in the jurisdiction crosswalk.

### Multi-Family

Period = **"QTD 2026"** only. No further date definition in the PDF. Market Metrics table has no
"as of" date label. Period is underdetermined from PDF alone — same external-source problem as
Prior 12 Months (see below).

---

## Open Decision — MUST RESOLVE BEFORE BACKFILL

### Period-stamping problem

The PDF contains **NO explicit quarter-end date anywhere.**

- "QTD 2026" is printed but never defined (no "Quarter to Date ending March 31, 2026").
- "Prior 12 Months" has no ending date appended to the header.
- Therefore `prior_12mo_ending` **CANNOT be derived from the PDF itself.**

**Three options — flag to Ricky:**

- **A. Filename convention** — derive report date from filename
  (e.g., `mhs-market-trends-Q1-2026.pdf`). Requires consistent naming on download. Fragile if MHS
  changes naming.
- **B. URL structure** — MHS website URL is `www.mhsappraisal.com/market-trends-2026`. If quarterly
  reports have URLs like `/market-trends-Q1-2026`, date can be parsed from URL at download time.
  Check MHS site before committing to this.
- **C. Manual stamp on ingest** — operator supplies `report_date` at load time as a required
  parameter. Most reliable. Least automated. Works for all historical PDFs regardless of naming.

**RECOMMENDATION:** Option C as the authoritative stamp, Option A or B as a cross-check.
**DO NOT ingest any rows with `prior_12mo_ending=NULL`.** Undated absorption is useless for a
rolling-window brain.

**THIS DECISION MUST BE MADE BEFORE DECADE BACKFILL. Flag to Ricky now.**

---

## Updated Open Items (replaces prior A–H list)

- **A. Bonita Springs regression — FIX FIRST.** 3 real rows (Retail 57,648 / Industrial 11,510 /
  Office (60,296)) confirmed by Copilot. NOT aggregate. Fix parser, restore rows.
- **B. Charlotte County** — real submarket rows in all 3 sectors. Going to unmapped by slug design.
  Values: Retail (61,132) / Industrial 211,532 / Office 16,420. **Flag to Ricky: slug or exclude?**
- **C. Period stamping decision** — see above. **Block on Ricky's call before any write.**
- **D. Jurisdiction crosswalk for permits** — build separately from submarket slugs.
  Known jurisdictions: Unincorporated Lee County, City of Cape Coral, Unincorporated Collier,
  City of Naples, City of Marco Island, Unincorporated Charlotte County, City of Punta Gorda,
  Town of Fort Myers Beach. **Do NOT reuse submarket slug table.**
- **E. MHS report cadence — annual or quarterly?** Check `www.mhsappraisal.com` before building
  backfill downloader. If quarterly, `prior_12mo` windows from consecutive reports overlap by
  9 months. If annual, windows are non-overlapping. **This changes the brain's time-series logic.**
- **F. Oldest historical PDF test** — before enabling decade backfill, test geometry header-match
  against oldest available PDF. Earlier templates may use "Net Absorption" or split headers.

---

## Confirmed Clean — Ready to Write When Period Decision Resolves

- **Vacancy:** 42/42 geometry-confirmed (geometry + counting rule agree, independent confirmation).
- **Rent:** 42/42 geometry-confirmed.
- **Absorption:** geometry-confirmed for all 3 sectors once Bonita Springs regression is fixed.
- **Row count after fix:** 15/sector minimum (Charlotte County slug decision still open).

---

## Blocking Status (end of append)

**Do NOT write rows.** Period-stamping decision (items C + E) is the blocking item — flagged to
Ricky. Bonita Springs fix may be **written but not loaded** until the period decision resolves.

---

# Build Decisions Locked — 2026-06-05 (late)

## Decided

- **Bonita Springs regression FIXED** in `dump_tables.py`: the `sub_y + 45` hardcoded data-region
  floor was eating the first data row (Bonita sits at y≈254, inside the 267.7 cutoff). Replaced with
  a **dynamic row-interval** (each row bounded by the midpoint to its neighbours; first/last row
  mirrors its single neighbour's half-pitch — no hardcoded offset). Restored Bonita in all 3 sectors
  **and** repaired the truncated `Collier` → `Outlying Collier County` label as a side effect.
- **Row count = 16/sector, 48 total** (not 15/45). The PDF genuinely has 16 submarkets/sector. The
  old `_build_geometry` "42/42" was really 14 rows — it silently dropped Bonita + mangled Collier.
- **Full 48-row validation vs hand-keyed GT:** vacancy **48/48 CLEAN**, rent **48/48 CLEAN**,
  absorption 31/48 — the 17 mismatches are all rows where **GT is the buggy side** (UC-into-absorption
  hand-key errors; 16 of 17 are retail). Geometry is authoritative; GT absorption is not.
- **Decision B (Ricky):** include all 16 rows/sector. `Charlotte County` + `Outlying Collier County`
  tagged `geographic_type: county`; other 14 `submarket`. Written into `submarket-slugs.yaml`.
- **Decision (a):** extend `marketbeat_swfl` (no new table) + new writer. Migration drafted at
  `docs/sql/20260605_marketbeat_swfl_mhs_extension.sql` (12→13 cols, idempotent, ADD COLUMN IF NOT EXISTS).
- **`verified` retained, NOT dropped.** Read-filtered by `cre-swfl.mts` (verified=eq.true) +
  `marketbeat-swfl-source.mts` (.eq("verified", true)), written by n8n. Writer sets
  `verified = verified_vacancy AND verified_rents AND verified_absorption`.
- **Verified strategy (Ricky):** `verified_vacancy=true`, `verified_rents=true`,
  `verified_absorption=false` → all 48 stage **dark** (`verified=false`) until Ricky signs off
  absorption from `dump_all_review.csv`. **Do NOT assume the absorption flip — wait for explicit word.**

## [INFERENCE] — period stamp (Consumption Contract v2.1)

- **`prior_12mo_ending = '2026-03-31'` is an INFERENCE, not a PDF-confirmed date.** (Date value is
  operator-locked; only the provenance string was reworded — see 2026-06-05 update below.)
- **Provenance (reworded 2026-06-05):** strongest anchor is the MHS 2026 Data Book **pub date
  2026-03-13** (beats the `/2026/03/` URL guess), plus URL path `/2026/03/` + page title "QTD 2026".
  MHS prints no explicit period-end; cadence (annual vs quarterly) unconfirmed (item C). The string now
  carries a **re-check range** so a future session knows when to re-verify. Canonical value (all 48 live
  rows UPDATEd to it; **load_mhs.py off-main must mirror it verbatim**):
  `prior_12mo_ending_source = 'inferred 2026-03-31; anchor = MHS 2026 Data Book pub date 2026-03-13 + URL path /2026/03/ + "QTD 2026" title (MHS prints no explicit period-end). Cadence unconfirmed (annual vs quarterly, item C). RE-CHECK mhsappraisal.com each quarter 2026-Q2..2027-Q1; if a newer report or explicit period-end appears, update prior_12mo_ending + quarter. If none by 2027-03, treat as annual and 2026-03-31 stands.'`
- **Falsifier / re-check:** re-verify on `mhsappraisal.com` each quarter 2026-Q2..2027-Q1; if the site
  confirms a different period-end (or a newer report drops), update `prior_12mo_ending` + the derived quarter.
- **Item C remains OPEN** until MHS cadence/period-end is confirmed from their website. This reverses
  the earlier NULL stance (Ricky's call); session-log item D had said _do not hardcode 2026-03-31_ —
  recorded here so the override is visible, not silent.

## Open / Verify next time

- **ALTER TABLE:** Ricky runs `20260605_marketbeat_swfl_mhs_extension.sql` manually in the Supabase
  SQL editor. CT does NOT execute via service key. Writer build + dry-run only AFTER Ricky confirms.
- **Absorption sign-off** pending Ricky's review of `dump_all_review.csv` → then flip
  `verified_absorption=true` (explicit word required).
- **Housekeeping:** fold the `sub_y` dynamic-interval fix into `_build_geometry.py` (still has the dead
  `+45` floor) when building the writer.

---

# Writer + Consumer Build — 2026-06-05 (later)

## Decided / Done

- **Decision (c):** cre-swfl is **retail-only**; all 48 rows (3 sectors) stored, only retail surfaced.
  Option (b) cross-sector blending REJECTED (incoherent demand drivers/lease structures). Option (a)
  per-sector surfacing = separate brain-design PR, out of scope now.
- **New writer `shadow-janitor-ref/load_mhs.py`** (separate from `load_csv.py`). Reads the 3 geometry
  dumps, resolves slug + `geographic_type`, stamps locked metadata, upserts the full MHS column set.
  `id = source_name '_' sector '_' submarket '_' quarter` (4-part — avoids both the sector and the
  source_name collisions on the shared submarket/quarter grain).
- **Dry-run CLEAN:** 48 rows, `verified=true` (vac+rent+abs all true), stamps correct, slug/geo correct.
  `source_url` is a flagged PLACEHOLDER — needs the real PDF asset URL before live write.
- **Consumer patched (same PR), retail-only via `const LEGACY_SECTOR = "retail"`:**
  - `marketbeat-swfl-source.mts`: `fetchLive()` selects `sector` + `.eq("sector","retail")`;
    `MarketbeatRow.sector?` + `MarketbeatSwflNormalized.sector?` added (optional → fixtures/tools/tests
    that predate the column still type-check; live source always populates); dedup map keyed on
    `sector + '_' + submarket`; `idKey` includes sector.
  - `cre-swfl.mts`: both citation URLs (agg + per-submarket) gained `&sector=eq.retail`.
  - **Tests green:** `bun test` 31/31 pass (marketbeat-swfl-source + cre-swfl).
- **`_build_geometry.py` floor fix folded in** → harness now reports **vacancy 48/48, rent 48/48**,
  absorption 31/48 (the 17 GT mismatches are GT's UC-into-absorption hand-key errors). The dead `+45`
  floor is gone from the harness too.
- **`/checks` opened:** `cre_swfl_per_sector_surfacing` (project cre-swfl, priority 2) — the option-A
  reminder so industrial/office surfacing isn't forgotten.

## [BLOCKER → Ricky] Migration contradiction — resolve before live write

- The migration file's O5 note says **`UNIQUE (submarket, quarter)` is LEFT UNCHANGED**; LittleBird's
  live-DB check said **`UNIQUE(sector, submarket, quarter)` is already swapped in**. **These conflict.**
- O5's proposed `UNIQUE(submarket, quarter, source_name)` **omits `sector`** → the 3 sectors collide.
  Correct natural key is **4-part: `(source_name, sector, submarket, quarter)`** (matches the writer's id).
- **Action:** confirm the live UNIQUE constraint and align it to the 4-part key before the upsert, or the
  write errors / silently collapses.

## [INFERENCE wrinkle] annual vs quarterly

- Migration edit now frames MHS as **ANNUAL** ("2026 Data Book, pub 2026-03-13"). That tension with the
  locked `prior_12mo_ending='2026-03-31'` / `quarter='2026-Q1'` stamp feeds **item C** (cadence). Pub
  date `2026-03-13` is a stronger anchor than the `/2026/03/` URL guess — Ricky may want to update the
  provenance string. Stamp left as locked until he says otherwise.

## Live-write gates — ALL CLEARED ✅ (SHIPPED 2026-06-05)

1. ✅ 13 ALTERs applied (LittleBird) + 4-part `UNIQUE(source_name, sector, submarket, quarter)` confirmed.
2. ✅ Old 48 wrong-absorption rows (all `cw_marketbeat` default-tag, zero genuine C&W) removed by scoped
   `DELETE WHERE source_name='cw_marketbeat'` (LittleBird) → table at 0.
3. ✅ `source_url` resolved to the real MHS article page
   (`…/southwest-florida-commercial-real-estate-in-2026-what-the-data-actually-shows/`, pub Mar 19 2026).
   The data-book PDF is NOT publicly linked anywhere on the MHS site (scraped, `links: []`) — article is
   the honest public citation; no invented `/wp-content/.../*.pdf` filename.
4. ✅ **`load_mhs.py` run live by CT via service key (option A).** `[WRITTEN] 48`.
5. ✅ **Verified against live DB:** 48 rows, `mhs_databook` × {retail 16, industrial 16, office 16},
   48/48 unique on the 4-part key, all `verified=true` + 3 per-field flags true, all
   `prior_12mo_ending='2026-03-31'` / `report_label='QTD 2026'`, geo 42 submarket + 6 county
   (Charlotte + Outlying Collier × 3). cre-swfl sees retail-only (16 rows) via `sector=eq.retail`.

## Done after ship

- **`source_name` DROP DEFAULT** documented in the migration (`…20260605…_mhs_extension.sql`): the
  placeholder `'cw_marketbeat'` default removed so a writer omitting `source_name` fails loud (NOT NULL).
  Safe — n8n C&W flow killed in PR #41 (never inserted). **Ricky runs the one-liner in Supabase** (DDL).

## Still open (post-ship)

- **Item C (cadence):** annual vs quarterly unconfirmed; `2026-03-31` stamp is the locked [INFERENCE].
- **`/checks` `cre_swfl_per_sector_surfacing`:** option-A per-sector surfacing (industrial/office stored,
  not surfaced) — separate brain-design PR.
- **DROP DEFAULT** ALTER pending Ricky's SQL-editor run (documented, not yet applied live).
