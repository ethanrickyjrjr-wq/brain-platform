import GraphLoader from "./GraphLoader";

export const metadata = {
  title: "Brain Graph — SWFL Data Gulf",
  description: "3D knowledge graph of the SWFL brain network",
};

export default function GraphPage() {
  return <GraphLoader />;
}
